# src/portfolio/

## 목적
복수 전략 출력을 **한 곳에서 조립** 해 포트폴리오 리스크 모듈 (`src/risk/`) 로 흘려보내고, 주문 의도를 정책과 최신 리스크 리포트에 대해 게이팅하는 레이어.

## 구조
- `orchestrator.py` — `StrategyOrchestrator` (sync stub). 전략 수익률 등록 → LW Σ / CVaR / ENB 리포트 갱신 → `evaluate()` 를 통한 주문 판정. `compute_fear_greed_proxy(price_history, window=252)` 헬퍼 (#87 P4) — 가격 기반 `close_latest / rolling_max(window)` 정규화, 소셜 크롤 없음.
- `__init__.py` — 공개 API.

## 역할
- 전략 N개의 일수익률 → T×N DataFrame → `risk.compute_portfolio_risk_from_df`
- 생성된 `PortfolioRiskReport` 를 `Snapshot.portfolio_risk` 로 주입
- 가격 히스토리 → `compute_fear_greed_proxy` → `Snapshot.fear_greed_proxy` 로 주입 (`evaluate_order()` 에서 조립, `refresh_portfolio_risk()` 시그니처 불변)
- `risk.evaluate(policy, snap)` 한 번 호출로 per-order 판정 + per_portfolio_risk 판정을 **같은 precedence chain** 에서 처리 (`extreme_fear_block` 평가 시 `snap.fear_greed_proxy` 직접 참조)

## 현재 스코프 (#70, #76 에서 제공)
- Sync API: `register_strategy_returns`, `refresh_portfolio_risk`, `evaluate_order`
- `strategy_reliability_score(strategy_id)` → [0,1]: `convex_base × drawdown_gate`. NaN 가드: T<20 → 0.0. KR101139626B1 differs: 곱셈 게이트(불연속 지시함수) ≠ KR 가산합.
- **reliability metric**: drawdown_gate = clip(1 − max_dd/0.20, 0, 1). max_dd ≥ 20% → 하드 제로.
- 2+ 전략이 있어야 report 생성. 1전략 이하 / T<2 → `ALLOW` 경로 유지 (회귀 zero)

## 후속 스코프 (#78 에서 확장)
- asyncio 이벤트 루프 + `run_bar(ts, market_snapshot)` tick driver
- `Signal → PositionSizer` 배선 (#69, #76 의존)
- `OrderIntent` 배치 emit + idempotency-key 스레딩
- 전략 예외 격리 / circuit-breaker

## 운영 규칙
- **주문·리스크 결정을 LLM 에 위임 금지** — CLAUDE.md 불변식 #6.
- **리스크 평가는 `risk.evaluate()` 만** — 자체 룰 재구현 금지. 경로 우회 시 `rule_id` 라벨 공간 오염.
- 새 전략 추가 시 `register_strategy_returns(strategy_id, series)` 로만 진입. 공분산 계산/ENB 계산 직접 호출 금지.

## 관련
- `src/risk/` — consumer of `Snapshot.portfolio_risk`
- `src/backtest/strategies/` — 전략 구현체 (수익률 시계열 제공자)
- `docs/background/09-system-components.md` §2 파이프라인 박스 (StrategyEngine → Sizer → RiskManager)
- `docs/background/19-portfolio-risk.md` §6 v2 delivered
- 이슈 #70 (본 스텁), #78 (async 확장), #79 (카탈로그), #80 (라이브 루프), #87 (fear_greed_proxy + cvar_levels 배선)
