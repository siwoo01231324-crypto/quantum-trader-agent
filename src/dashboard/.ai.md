# src/dashboard/

## 목적
FastAPI 로컬 대시보드 — localhost:8000 에서 4사분면 HTML UI 와 Prometheus `/metrics` endpoint 제공.

## 구조
| 파일 | 역할 |
|------|------|
| `app.py` | FastAPI 앱 팩토리 `create_app(state)` + standalone `app` 인스턴스. WS `/ws/timeline`, `/strategies`, `/api/run/*`, `/api/account/info` 등 포함. **#238 follow-up**: `/api/strategy_positions` 가 `state.price_cache` (LivePriceCache, mark-price feed 1Hz writer) 에서 mark_price + pnl_pct (LONG/SHORT 부호 자동) overlay. 신규 endpoint `POST /api/strategies/{sid}/positions/{sym}/close` — 보유 종목 시장가 청산 (NET LONG→SELL, NET SHORT→BUY cover, `reduce_only=True` 강제). `state.manual_close_executor` closure 미주입 (paper/dashboard-only) 시 503. **청산 후 `orchestrator.sync_live_entered(sid,sym,remaining)` 호출 (2026-05-22)** — 수동 청산은 store·broker 가 함께 flat 이라 reconciler mismatch 를 안 만들어 PR #287 sync 가 안 걸리고 release_live_position(stop/TP 경로)도 안 거친다 → 미정리 시 그 (전략,종목) 재진입 영구 차단. remaining=0 → discard, 부분청산 → 유지. UI 의 전략별 포지션 카드에 손익% 컬럼 + 청산 버튼 추가 (`.stratpos-close-btn`, confirm dialog, 5초 후 자동 새로고침) |
| `timeline_broker.py` | in-process pub/sub. drop-oldest back-pressure (#181) |
| `timeline_events.py` | 매매 타임라인 4단계 `event_type` 상수 (#181) |
| `strategy_catalog.py` | `docs/specs/strategies/*.md` frontmatter 로더 (#178). 정규화된 dict list 반환 |
| `run_controller.py` | 거래 시작/정지 컨트롤러 — asyncio task 핸들 + 상태 머신 (#182) |
| `account_info.py` | KIS + Binance 계좌 인증·잔고 조회 + 15s TTL 캐싱 (#182, TTL #231). **thread-safe single-flight** (prior-review MEDIUM #2): `fetch()` 가 대시보드 `/api/account/info` worker thread + 라이브 `_inject_real_equity` 에서 동시 호출됨 → `_state_lock`(짧은 캐시 read/write) + `_refresh_lock`(miss 시 1회만 REST, 후속 caller 는 in-flight 결과 재사용). warm-cache read 는 `_state_lock` 만 → slow REST 뒤에 직렬화 안 됨. `peek()` = REST 없는 non-blocking 캐시 read (#3 off-loop seam 용). 동작/반환 shape 불변 |
| `shadow_runs.py` | `logs/shadow/{run_id}/wal.jsonl` 디렉토리 read-only 스캔 — discover/load_detail + 거래소·봉·alive 자동 분류 (#198) |
| `cs_tsmom_signals.py` | cs-tsmom-crypto-daily 자체 score 계산 + cross-sectional 랭킹 (`/cs-tsmom` 페이지 백엔드). **2026-05-21 fix**: universe 가 `src/portfolio/binance_universe.BINANCE_USDT_TOP30` 단일 소스를 import (이전엔 `fetch_top_universe(30)` 으로 매번 다른 universe 잡아 메이저 누락 사고). `_refresh()` 가 캐시 없거나 길이 < 252+2 인 종목은 자동 `refresh=True` 강제 재페치. row 별 `reason: ok/no_data/warmup/low_volume/negative_score/out_of_top_n` 필드 추가로 UI 에서 사용자가 왜 OUT 인지 즉시 가시. `CsTsmomState.pin_date` 도 응답에 포함 — 페이지 헤더에 `universe pin: 2026-05-21` 뱃지 |
| `__init__.py` | 패키지 |

## UI 레이아웃 (#238 redesign — Binance Futures 스타일)

스티키 상단 헤더 바 + 6개 수직 섹션으로 재편. 모든 수치는 `IBM Plex Mono` 모노스페이스.
팔레트: bg `#0b0e11`, surface `#161a1e`, green `#0ecb81`, red `#f6465d`, yellow `#f0a500`.

| 섹션 | 내용 |
|------|------|
| 1. PnL 요약 | 통합 스칼라 2-col 카드 (일간·월간) + venue 분리 2-col (KIS KRW / Binance USDT 각각 일간·월간). KRW·USDT 합산 불가 명시. JS `pnlVenueRefresh()` 5초 폴링. **"실시간" 칸 2026-05-22 제거** — 미실현은 Binance 계좌 카드 hero(`bnb-upnl`) + 전략별 포지션 row 에서 노출되므로 redundant |
| 2. 계좌/실제 포지션 | Binance Futures 히어로 카드 (지갑/가용/uPnL) + 열린 포지션 테이블 (LONG/SHORT badge) + KIS paper 카드. 거래 제어 버튼 KIS 카드 하단 내장 |
| 3. 전략별 포지션 | sticky 헤더 + zebra 행 + LONG/SHORT/FLAT 뱃지 + 순포지션 색상 + 실현손익 |
| 4. 타임라인 / 한도·킬스위치·Ops | 2열: 좌=타임라인(연속 동일 이벤트 ×N 그룹 접기), 우=한도 게이지(4px 바)+비상정지+Ops 진단 카드 |
| 5. 거래 이력 (fills) | sticky 헤더 + 수량/가격 monospace 우정렬 |
| 6. 거래 내역 (round-trip) | `reconstruct_trades` 기반 진입/청산 페어 테이블. 진입시각·청산시각·보유시간·전략·종목·venue·방향·수량·진입가·청산가·실현손익(통화 표기)·상태. 보유중=amber "보유중" 뱃지, 청산됨=green "청산됨" 뱃지. **최근 활동 순 정렬** (청산 거래는 exit_ts, 미청산은 entry_ts 내림차순 — 2026-05-22; 기존 entry_ts-only 는 cross-run cost-basis 페어링 탓에 방금 청산한 거래가 목록 중간에 묻혔다). 200건 캡 + 초과 시 노트. JS `tradeHistoryRefresh()` 10초 폴링 |
| 7. 전략 카탈로그 | 기존 카드 그리드 (디자인 업데이트) |

### 타임라인 그룹화 로직
- 80ms debounce 버퍼로 연속 도착 이벤트를 모아, 동일 `event_type` 연속 반복은 `×N` 그룹 뱃지로 접음
- 그룹 뱃지 클릭 시 alert 로 상세(count/first ts/last ts) 표시
- DOM cap 120행

## Endpoints
| Method | Path | 설명 |
|--------|------|------|
| GET | `/` | 4사분면 HTML (5초 자동 새로고침, Q3 는 WS 로 incremental) |
| GET | `/metrics` | Prometheus exposition format |
| GET | `/api/pnl` | 손익 JSON (realized). **2026-05-23 — venue 별 source 분리.** Binance = 거래소 income 원장(`AccountInfoProvider.fetch_binance_pnl` → `/fapi/v1/income` 의 REALIZED_PNL+COMMISSION+FUNDING_FEE NET) — 거래소 화면과 정확히 일치. KIS = `reconstruct_trades` 재구성 추정치 (income TR 연동은 후속). `daily_by_venue/monthly_by_venue` (dict, key="binance"/"kis", 통화별 — 절대 합산 금지) + top-level `daily/monthly` 스칼라(= Binance venue 값, telegram `/today` 등 단일 숫자 요약용) + `by_strategy` (재구성). 실패 venue 는 null, never 500 |
| GET | `/api/trade_history?limit=N` | Round-trip 거래 내역 JSON. `discover_wal_files(log_dir)` → `reconstruct_trades`. `{trades, total, truncated, log_dir_used}`. `realized_pnl` 은 venue 통화 기준, 크로스-합산 금지. **영구·누적** — `_resolve_log_dir()` 가 모든 run 의 WAL 을 합산 (#238 follow-up) |
| GET | `/api/signals?venue=binance&limit=N` | 신호 목록 JSON (#268). `signal_emitted` WAL 이벤트만 추출, venue=binance 면 USDT 종결 심볼만. 각 row 에 `follow_up`(filled/ordered/pending) — `_resolve_followup` 가 같은 (strategy_id, symbol, side) 의 후속 `fill_received`/`order_acked` 를 120s 윈도 안에서 매칭. 영구·누적 + 디렉토리 부재 graceful |
| GET | `/signals` | 신호 목록 HTML 페이지 (#268). `_render_signals_page()` — sticky 헤더 테이블 + 5초 폴링. buy=green / sell=red 뱃지, follow_up=filled/ordered/pending 뱃지 |
| GET | `/cs-tsmom` | cs-tsmom-crypto-daily 랭킹표 HTML — 30종 score (12-1m TSMOM) + rank + signal (ENTER/HOLD/EXIT/OUT) + 사유 컬럼. 우상단에 universe pin-date 뱃지 + ↻ 강제 새로고침 버튼. **2026-05-21 fix**: 사유 컬럼 추가 (`no_data`/`warmup`/`low_volume`/`negative_score`/`out_of_top_n`/`ok`) + universe pin 표시 |
| GET | `/api/cs-tsmom` | cs-tsmom 신호 JSON. `CsTsmomComputer.compute()` (1h TTL 캐시). `{available, fetched_at, universe_size, pin_date, rows[{symbol, score, rank, signal, in_top_today, liquid, reason, ...}]}`. NaN → None (Starlette allow_nan=False 회피) |
| POST | `/api/cs-tsmom/refresh` | 캐시 무효화 + 강제 재계산 (2026-05-21 fix). `compute(force=True)` 호출. 응답 `{ok, available, fetched_at, universe_size, pin_date, row_count}`. UI 의 ↻ 버튼이 호출. single-flight 락으로 동시 호출 직렬화 |
| GET | `/manual` | 수동 거래 입력 폼 HTML (2026-05-21). Binance/KIS 에서 손으로 거래한 내역 + 진입/청산 근거 메모 입력. `_render_manual_page()` |
| POST | `/api/manual_trade` | 수동 거래 1건 append. body `{symbol, side, kind, qty, price, venue, note, ts?}`. `_resolve_log_dir()/manual_trade.jsonl` 에 WAL 동일 schema 로 JSONL append. event_type=`manual_trade` |
| GET | `/api/manual_trade/today` | 오늘(KST 자정~) 수동 거래 list. `/manual` 페이지 + 일일 리포트 routine 이 사용 |
| GET | `/api/journal/today` | **Claude Routines 일일 리포트 통합 endpoint** (2026-05-21). 오늘 KST 자정~지금 사이의 `auto_fills` (WAL `order_filled`/`fill_received`) + `auto_signals` (WAL `signal_emitted`) + `manual_trades` (manual_trade.jsonl) + `cs_tsmom_top10` 모두 1건 JSON 으로 노출. routine 이 단일 fetch 로 분석 데이터 확보. `docs/routines/cs-tsmom-daily-report.md` prompt 가 사용 |
| GET | `/api/strategy_positions` | 전략별 매수/매도 집계 JSON. **체결(`order_filled`/`fill_received`)만 집계** — `order_acked` 는 같은 주문의 NEW phase 라 제외 (포함 시 acked+filled 둘 다 세어 buy_n/buy_qty 2배, 2026-05-22 INJ 862 사고). **영구·누적** — `_resolve_log_dir()`+`discover_wal_files()` 로 모든 run WAL 합산 후 현재 run wal_path/extra_wal_paths union (이전엔 현재 run 만 보여 매 run wipe 됐음, #238 follow-up). 디렉토리 부재 → `{strategies:[]}`, never 500 |
| GET | `/api/venue_equity_status` | venue 실증 상태 JSON (#238 follow-up). `state.snapshot_builder.last_equity_status` 노출. `{available, venues:{venue:{ok,reason,equity}}}`. `ok:False` = 해당 venue real equity 미확보 → 주문 전량 보류 (silent-drop 가시화). builder 미주입/이상 → `{available:False, venues:{}}`, never 500 |
| GET | `/api/limits` | 한도 사용률 JSON |
| GET | `/api/kill-switch` | 킬스위치 상태 JSON |
| POST | `/api/kill-switch/trigger` | 킬스위치 트리거 발동 |
| POST | `/api/kill-switch/reset` | 킬스위치 트리거 해제 |
| GET | `/strategies` | 전략 카탈로그 HTML (#178). 카드 그리드 + 카드별 토글 스위치 (#180) |
| GET | `/api/strategies` | 전략 카탈로그 JSON. frontmatter + orchestrator `enabled` 머지 |
| POST | `/api/strategies/{id}/toggle` | 전략 ON/OFF 토글 (#180). body `{enabled: bool}`. 비활성 시 보유 포지션 `OrderIntent(side='sell')` list 를 `liquidation_intents` 로 반환 |
| WS | `/ws/timeline?replay=N` | 매매 타임라인 실시간 스트림 (#181). 연결 시 WAL 마지막 N건 replay → `live_ready` 센티넬 → broker fan-out |
| GET | `/api/run/status` | RunController 상태 (stopped/starting/running/error) (#182) |
| POST | `/api/run/start` | 거래 시작 (#182). body: `{symbols?, broker?, duration?}`. 기본은 production.yaml 첫 KRX 전략 |
| POST | `/api/run/stop` | 거래 정지 (#182) |
| GET | `/api/account/info` | KIS + Binance 잔고 조회 (5초 캐싱). dashboard "내 계좌" 카드용 (#182) |
| GET | `/shadow_runs` | Shadow Runs HTML 페이지 — Binance/KIS WAL 통합 카드 그리드 (#198) |
| GET | `/api/shadow_runs` | Shadow Runs 목록 JSON — `logs/shadow/*` 스캔 (#198) |
| GET | `/api/shadow_runs/{run_id}` | Shadow Run 상세 JSON — broker 상태 복원 + 포지션 + 잔고 (#198) |

## 전략 카탈로그 + 토글 (#178 + #180)

### 데이터 흐름
1. `load_strategy_catalog(specs_dir)` → `docs/specs/strategies/*.md` frontmatter → list[dict].
2. `_enriched_catalog()` 가 각 항목에 `enabled = orchestrator.is_enabled(id)` 머지. orchestrator 미연결 시 모두 `True`.
3. `/strategies` HTML 은 카드 그리드 + 토글 스위치, `/api/strategies` 는 JSON.

### 토글 시 흐름 (D1: 즉시 청산 — 사용자 결정 2026-05-05, CLAUDE.md 불변식 6)
1. 사용자가 카드 OFF 토글 클릭 → JS `confirm()` 다이얼로그 (D2).
2. 확인 시 `POST /api/strategies/{id}/toggle {enabled: false}`.
3. 핸들러는 `state.position_provider(id)` 로 보유 포지션 조회 (callback, 미연결 시 빈 list).
4. `orchestrator.disable_strategy(id, positions=...)` 호출 — `_disabled` set 등록 + WAL `strategy_toggled` 이벤트 emit + 시장가 매도 `OrderIntent` list 반환.
5. 응답에 `liquidation_intents` 포함 — 실제 broker 발주는 caller (live loop) 책임. 본 PR 에서는 **wiring 만**, broker 발주 통합은 후속 이슈.

### DashboardState 신규 필드
- `orchestrator: AsyncStrategyOrchestrator | None` — toggle 핸들러가 enable/disable 호출에 사용. None 이면 503.
- `specs_dir: Path | None` — 기본은 repo root 의 `docs/specs/strategies/`.
- `position_provider: Callable[[str], list[tuple[str, float]]] | None` — 전략 id → 보유 (symbol, qty) 리스트. `scripts/live_run.py` 가 `StrategyPositionStore.get_positions` 로 와이어링 (#192). 토글 OFF 시 caller 가 broker 로 sell intent 발주.

## WebSocket 타임라인 (#181)
- 4 종 `event_type`: `signal_emitted`, `metalabeler_decision`, `order_placed`, `fill_received` (`timeline_events.py`)
- 페이로드 구조 = `WALEvent` JSON dict (`ts`, `event_type`, `schema_version`, `payload`)
- `TimelineBroker` 가입자별 `asyncio.Queue(maxsize=100)` — 가득 차면 oldest drop
- WAL → broker 연결: `WAL(path, observer=broker.publish_walevent_as_dict)` 형식. EXE 런타임 와이어링은 `scripts/live_run.py:_run_pipeline` 가 담당 (#177): `DashboardState(wal_path, timeline_broker)` 구성 → `ShadowConfig.wal_observer = lambda ev: broker.publish(asdict(ev))` 주입 → `_start_dashboard` 가 uvicorn `Server.serve()` 를 메인 asyncio task 안에 띄움
- HTML JS: 연결 끊기면 1초 후 재연결, DOM 100건 cap

## Shadow Runs 뷰어 (#198)

### 데이터 흐름
1. `discover_shadow_runs(state.shadow_log_dir)` → `logs/shadow/*/wal.jsonl` 스캔.
2. 각 run 의 WAL replay → events / corruptions / 거래수 / 마지막 활동 시각 집계.
3. `classify_exchange/timeframe/alive` 으로 거래소·봉·생존 상태 분류.
4. `/shadow_runs` HTML 카드 그리드 + `/api/shadow_runs` JSON.
5. 상세는 `/api/shadow_runs/{run_id}` — `PaperBroker.from_wal` 로 포지션·잔고 복원.

### 분류 규칙
- **거래소**: `BTCUSDT`/`ETHUSDT` 등 USDT 종결 → binance, 6자리 숫자 → kis, 그 외 → unknown
- **봉**: `r4-switch` → 4h, `r6-switch` → 1h, `s2c-voltarget`/`s4-funding` → 4h, `kis`/`krx` 포함 → EOD
- **alive**: 마지막 이벤트가 (봉 단위 × 1.5) 이내 → alive, 봉 × 3 초과 → dead, 그 외 → idle

### Read-only 보장
- WAL 쓰지 않음. 스캔만.
- 디렉토리 부재 / WAL 부재 / corruption → graceful (`status: idle`, `error` 필드)
- broker replay 실패도 graceful — 요약만 표시

### DashboardState 신규 필드
- `shadow_log_dir: Path | None` — `logs/shadow/` 루트. None 이면 `<repo_root>/logs/shadow` 자동 해석.
- `log_dir: Path | None` — `/api/trade_history`·`/api/strategy_positions` 용 WAL 루트 (영구·누적의 핵심). `_resolve_log_dir()` 우선순위: ① `state.log_dir` (live_run.py 가 `wal_path.parent.parent` 로 항상 set) → ② `state.wal_path.parent.parent` → ③ `./logs/live` (CLI `--log-dir` default, standalone dashboard 도 과거 이력 노출, #238 follow-up) → ④ None. (run WAL = `{log_dir}/{run_id}/wal.jsonl` 약속).
- `snapshot_builder: Any | None` — live `SnapshotBuilder` (read-only). `/api/venue_equity_status` 가 `.last_equity_status` 노출 → venue INERT 가시화 (#238 follow-up). live_run.py 가 `ShadowConfig.on_snapshot_builder_ready` 콜백으로 wire (orchestrator 와 동일 패턴).

## 실행
```bash
uvicorn src.dashboard.app:app --host 0.0.0.0 --port 8000 --reload
```

## 의존성
- `src/observability/metrics.py` — Prometheus `Metrics` 객체 (registry 공유)
- `fastapi`, `prometheus-client`, `uvicorn`

## 규칙
- `DashboardState` 를 통해 런타임 데이터 주입 — 직접 전역 상태 수정 금지
- `/metrics` 는 `src/observability/metrics.py` 의 registry 그대로 노출 (이중 등록 금지)
- LLM 위임 금지 (CLAUDE.md 불변식 #6)

## 이슈
- #125: 최초 구현
- #181: WS `/ws/timeline` + TimelineBroker + WAL observer 연동
- #177: EXE 통합 — `qta.spec` hiddenimports 추가, `scripts/live_run.py --dashboard-port` 플래그 + 백그라운드 uvicorn task + WAL observer wiring
- #182: 더블클릭 UX — `_run_dashboard_only_mode` 자동 기동 + 자동 브라우저, RunController 기반 [거래 시작]/[정지] 버튼 (Q5), KIS+Binance 계좌 카드 (Q6/Q7), `.env` dotenv 자동 로드
- #198: Shadow Runs 뷰어 — `shadow_runs.py` + 라우트 3개 (`/shadow_runs`, `/api/shadow_runs`, `/api/shadow_runs/{run_id}`). `logs/shadow/*` read-only 스캔으로 #143 (R4 4h) + #199 (R6 1h) + 향후 #133 (KIS) 등 모든 shadow daemon 결과 통합 표시
- #192: `position_provider` 라이브 와이어링 — `src/live/strategy_position_store.StrategyPositionStore` 가 fill 이벤트를 누적하고 `state.position_provider = store.get_positions` 로 주입. 토글 OFF 시 그 전략의 실제 보유분이 sell intent 로 반환된다 (lambda 임시값 X)
- #194: 라이브 PnL 와이어링 — `state.pnl_aggregator` 필드 (`PnLAggregator`). `_enriched_catalog()` 가 각 전략 카드에 `pnl_today = aggregator.daily_for(sid)` 머지. (참고: 2026-05-23 PnL 카드 개편으로 `/api/pnl` 은 더 이상 aggregator 를 쓰지 않음 — 아래 항목.)
- **2026-05-23 PnL 카드 = 거래소 원장 직결**: PnL 일간/월간이 "실제 손익" 과 안 맞던 근본 원인은 대시보드가 실현손익을 *재구성* 했기 때문 (PnLAggregator cross-run cost-basis → reconstruct_trades round-trip — 둘 다 거래소 원장과 구조적으로 불일치: WAL 체결 누락 / 페어링 모델 차 / 수수료 정의 차). 해결 — Binance venue 는 거래소 income 원장(`/fapi/v1/income`) 직결. `BinanceFuturesClient.get_income()` (7일 윈도우 분할 + 1000건 페이징), `AccountInfoProvider.fetch_binance_pnl()` (이달~현재 income 1콜 → `aggregate_income_pnl` 로 일/월 NET, 30s 캐시). `/api/pnl` 의 Binance = income, KIS = 재구성 추정. "일간/월간 통합" 카드 제거 (KRW·USDT cross-sum 버그). 정적 `_pnl_view`/`pnl_daily`/`pnl_monthly` 제거. KIS income TR 연동은 후속 PR.
- #268: 신호 목록 페이지 (Binance) — `/signals` HTML + `/api/signals?venue=binance&limit=N` JSON. `signal_emitted` WAL 이벤트를 newest-first 로 나열, follow_up 컬럼이 (strategy_id, symbol, side) 매칭으로 120s 안의 `fill_received`/`order_acked`를 찾아 filled/ordered/pending 으로 분류 → 메타라벨러/리스크 게이트에 막힌 신호를 가시화. `_resolve_log_dir()` 재사용으로 모든 run 의 WAL 합산. 차트/마커 없음(표만, 클라이언트 5초 폴링).
- #238 (UI redesign): Binance Futures 스타일 전면 개편 — IBM Plex Mono/Sans KR 서체, `#0b0e11` 배경, `#0ecb81`/`#f6465d` long/short 팔레트, sticky 헤더 테이블, Binance 히어로 계좌 카드 (지갑/가용/uPnL 3-cell), 전략별 포지션 LONG·SHORT·FLAT 뱃지, 타임라인 ×N 그룹 접기 (80ms debounce + 연속 동일 이벤트 collapse). 모든 API/데이터 계약 불변.
- venue-split PnL + 거래 내역: `/api/pnl` 에 `*_by_venue` dict 추가 (KRW·USDT 분리, 합산 금지). `/api/trade_history` 신규 — `discover_wal_files(log_dir)` → `reconstruct_trades` → round-trip 페어 JSON. PnL 섹션 venue 2-col 카드 추가 (KIS KRW / Binance USDT 각각 실시간·일간·월간). 섹션 6 거래 내역 dense 테이블 추가 (보유중 amber 뱃지, 청산됨 green 뱃지, 통화 suffix, 200건 캡). `DashboardState.log_dir` 필드 추가 (None 시 wal_path.parent.parent 자동 유도).
