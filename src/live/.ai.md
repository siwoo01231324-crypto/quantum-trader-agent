# src/live/

## 목적

라이브 실행 프레임워크 (Phase 1 Shadow Paper). 실시간 WebSocket 시세 → orchestrator → PaperBroker → WAL/관측 파이프라인을 담당한다. 가상 체결(Paper) 단계이며 실자금 투입은 Phase 2(KIS 모의계좌) 이후다.

## 구조

현재 단계 (Phase C-1):

- `__init__.py` — 패키지 마커
- `types.py` — `OrderStatus`, `WALEvent`, `WALCorruption`, `Tick` 기반 타입
- `wal.py` — WAL write/replay + `WALWriteFailed`. fsync 성공 후 선택적 `observer(event)` 콜백 (#181, dashboard fan-out 용. 예외는 swallow + log warn)
- `process_lock.py` — 단일 프로세스 락 (filelock 기반)
- `schedule.py` (#216) — `wait_until_session_open(schedule, now_fn=, sleep_fn=)` async 헬퍼. `schedule='krx'` + 장 외 시간이면 다음 KRX 영업일 09:00 KST 까지 sleep, `'always'` / 장중이면 즉시 반환. `run_shadow_loop` 진입 직후 (ProcessLock 획득 전) 호출되어 마감 후 startup 시 KIS REST polling 폭주 (EGW00201) 를 사전 차단. `now_fn`/`sleep_fn` 주입으로 결정적 단위 테스트 가능.
- `conversion.py` — `intent_to_order_request` float→Decimal 변환 단일 지점
- `feed.py` — `MarketDataFeed` Protocol + `BinancePublicFeed` (USDT-M Futures public aggTrade WS, **단일 symbol** entry-trigger 용) + **`BinanceMarkPriceFeed`** (#238 follow-up — `!markPrice@arr@1s` all-symbol mark price 1Hz stream, **유니버스 전체 stop/TP evaluator**). aggTrade 와 markPrice 는 독립 WS 연결; 전자는 신규 진입 시그널 source, 후자는 보유 종목 자동 청산 평가 source. testnet endpoint(`DEFAULT_TESTNET=wss://stream.binancefuture.com/ws`) 사용 — 한국 IP 에서 mainnet `fstream.binance.com` 은 aggTrade/markPrice 모두 0건 push (지역 차단).
- `feed_kis.py` (#177) — `KISMarketFeed` (REST 1m polling, `inquire-time-itemchartprice`) + `MockReplayFeed` (deterministic smoke). KRX 휴장 / 장외 시간 가드: `is_krx_market_open`. **#227 S5**: `stagger=True` 옵션 (cycle 내 N 호출을 `poll_interval/N` 간격으로 분산 — 350종 운영시 KIS REST 분당 한도 회피) + `max_qpm` 옵션 (round-robin window 로 budget 강제). 풀 KIS WS market data subscribe 는 별 이슈로 분리 (실 API key + tr_id `H0STCNT0` 필요).
- `strategy_position_store.py` (#192) — `StrategyPositionStore`. fill 이벤트 → `dict[strategy_id, dict[symbol, Decimal qty]]` 누적. `register_order` 로 client_order_id ↔ strategy_id 매핑 유지, `replay_from_wal` 로 부팅 시 복구, `ingest_fill_event` 로 `wal_observer` 와 결합. `DashboardState.position_provider = store.get_positions` 로 와이어링되어 `/api/strategies/{id}/toggle` OFF 시 그 전략 보유분만 청산. **Binance live-fill gap**: `register_order_context(*, client_order_id, symbol, side, strategy_id)` + `resolve_order_context(coid) -> (symbol, side, strategy_id) | None` 추가 (additive — `register_order` 시그니처 불변, executor coid-attribution spy 호환 유지). Binance `BrokerFill` 은 coid + qty/price/fee 만 carry (symbol/side 없음) → `fill_consumer` 가 이 map 으로 full context 해석. in-memory map 은 cross-run 소실 → 영구성은 WAL `order_filled` payload 의 persisted `strategy_id` 가 담당 (#18 패턴 동일). **`sole_holder_strategy(symbol)` (2026-06-14)**: 해당 symbol 을 0 아닌 qty 로 보유한 전략이 *정확히 1개* 면 그 sid, 아니면 None(0명=수동/외부, 2명=다전략). 거래소 네이티브 TP/SL 청산처럼 coid 미등록 fill 의 strategy 귀속용 — `fill_consumer._resolve_fill_attribution` 이 사용. **근본버그 차단(2026-06-14)**: 네이티브 TP/SL 청산은 우리가 coid 를 등록 안 한 plan order 라 옛날엔 `order_filled` 가 `symbol=""` 로 기록 → replay 가 drop → 청산이 store 에서 차감 안 돼 숏이 매 run 누적(3~8× 인플레이션, AVAX/CL 처럼 청산된 종목도 잔존). 이제 `BrokerFill` 이 fill 자체 symbol/side(Bitget instId/side)를 싣고 단독보유 전략에 귀속 → 청산 보존.
- `pnl_aggregator.py` (#194) — `PnLAggregator`. fill 이벤트 → 평균매수가 (Decimal) 추적 + realized PnL 누적 (`realtime` / `daily` / `monthly` / `by_strategy` / `daily_for(sid)`). KST 09:00 business-date 기준으로 daily/monthly 분류 (자정 12시 X) — fill ts 검사 + property 호출 시 자동 reset. `replay_from_wal` 로 부팅 복구, `ingest_fill_event` 로 `wal_observer` 와 결합. `DashboardState.pnl_aggregator` 로 와이어링되어 `/api/pnl` + 카드 `pnl_today` 가 라이브 갱신. **#238**: `_apply_to_cost_basis` 가 SHORT 추적 — `sell` & held≤0 → 음수측 가중평균 진입가 (qty=0 0/0 가드: 미가드 시 broker zero-qty 정정 fill 이 aggregator 를 죽여 세션 P&L/리스크 게이팅 전체 정지), `buy` & held<0 → cover 실현 `(entry-cover)*qty`. 양방향 flip 처리 (short→long: `held>0 → avg=price`, long→short: `close_qty=min(qty,held)`, `held<0 → avg=price`). qty≤held LONG 매도 경로는 byte-identical legacy. **per-venue split (통화혼합 버그 수정)**: scalar `_cum_realized`/`_daily`/`_monthly` 은 KRW(KIS)+USDT(Binance) 를 무의미하게 단순합산했었다. 모듈 레벨 `classify_venue(symbol)` — `*USDT`(len>4)→`"binance"`, 6자리 숫자 KRX→`"kis"`, 그 외→`"unknown"`(방어적, 절대 raise 안 함: live P&L/대시보드 공급원이라 raise 시 전 venue 회계 정지). `record_fill` 이 기존 누적 *옆에* `_cum_by_venue`/`_daily_by_venue`/`_monthly_by_venue` 버킷 추가 (`_apply_to_cost_basis` 불변). 신규 accessor `realtime_by_venue()` / `daily_by_venue()` / `monthly_by_venue() -> dict[str,float]`. 기존 scalar/`by_strategy`/`daily_for` 는 bit-identical 유지 (271+ 기존 테스트 + `test_pnl_aggregator.py` 그대로 green). per-venue daily/monthly 는 동일 `_refresh_business_window`(KST-09:00) reset 사용. 테스트 `tests/live/test_pnl_aggregator_by_venue.py`.
- `trade_history.py` (per-venue split 동반) — cross-run round-trip trade 재구성. 순수·결정론, I/O 는 주어진 WAL path 를 기존 `wal.replay` 로 읽는 것 뿐. `discover_wal_files(log_dir) -> list[Path]` (run 별 `*/wal.jsonl` glob, path 정렬; 디렉토리 부재 → `[]`). `reconstruct_trades(wal_paths) -> list[Trade]` — `order_filled` fill 을 (strategy_id, symbol) 별로 `pnl_aggregator._apply_to_cost_basis` 와 **동일한** long/short+flip 회계로 페어링 (entry open/증가, 반대측 reduce/close, oversized 반대측 = flip = close 후 신규 open). strategy_id 해석은 `StrategyPositionStore._resolve_strategy` / `PnLAggregator._resolve_strategy` 와 동일 — payload `strategy_id` 우선, 아니면 `client_order_id` 의 `{strategy}:{symbol}:{ts}:{idx}` prefix (해석 불가 fill 은 drop+warn). `Trade` 프로즌 dataclass: strategy_id/symbol/venue/side(long|short)/qty/entry_ts/entry_price/exit_ts/exit_price/realized_pnl(venue 통화, **cross-convert 금지**)/holding_seconds/status(closed|open). 미청산 잔량 → `status="open"` 1건 (exit/pnl=None). 정렬 = entry_ts → symbol 결정론. 테스트 `tests/live/test_trade_history.py`.
- `position_reconciliation.py` (#238 Item 5 — safe core) — `reconcile_positions(logical, broker_net, *, tol) -> list[ReconcileMismatch]` + `sum_logical_by_symbol`. **불변식**: Binance one-way Futures = 종목당 net 포지션 1개. `StrategyPositionStore` 의 per-strategy 논리 포지션은 거래소 실재가 아닌 *귀속*일 뿐 — Σ(logical/symbol) 을 broker 실 net 과 대조해 |delta|>tol 인 종목을 표면화 (incident: 논리 -0.90 vs Binance -0.85; live-scanner +0.05 "롱" 은 momo 숏에 netting 돼 실재 안 함). 순수·결정론. **귀속 재설계 자체(논리↔거래소 정합 회계)는 design-first 별도 — 이 모듈은 silent divergence 가시화 안전망**.
- `position_reconciler.py` (#274) — `PositionReconciler` background task. 주기적 (default 60s) broker `get_net_positions()` ↔ `StrategyPositionStore` 비교 → mismatch 시 WAL `position_reconciled` + timeline alert. single-holder mismatch 는 `force_sync_position` auto-fix, phantom/multi-holder 는 알림만. **`on_position_synced` 콜백 (2026-05-22)** — auto-fix 직후 `(sid, symbol, broker_net)` 호출. `scripts/live_run.py` 가 `orchestrator.sync_live_entered` 로 연결 → store 를 0 으로 고칠 때 `_live_entered` set 도 같이 비움 (미연결 시 청산 종목 영구 진입 차단). 콜백 예외는 흡수 — reconcile loop 절대 안 죽음. 테스트 `tests/live/test_position_reconciler.py`.
- `snapshot_builder.py` (#177) — `SnapshotBuilder.warmup()` (KIS REST backfill) + `build_snapshot(tick) -> dict` (전략용 ohlcv_history / history / factors). 멀티 timeframe pandas resample 기반. **#238 미배선 (사람 결정 필요)**: orchestrator 의 fraction→qty 변환(`portfolio.sizing.size_to_qty`)은 venue-correct 자본을 요구 — KRX 6자리는 `snapshot["equity_krw"]`, Binance `*USDT` 는 `snapshot["equity_usdt"]`. 현재 `build_snapshot` 과 `loop.py::_tick_to_market_snapshot` 은 `equity_krw` 를 placeholder(100000.0)로만 채우고 `equity_usdt` 키는 미설정. **#238 Item 9 배선 완료**: `SnapshotBuilder(balance_provider=...)` DI 추가 (default None → config placeholder → byte-identical 하위호환). 주입 시 `build_snapshot._inject_real_equity` 가 `provider.fetch()` (= `AccountInfoProvider`, Binance `available_usdt` + KIS `cash_balance`, 내부 15s 캐시) 로 `equity_usdt`/`equity_krw` 를 실값 overlay — fetch 예외/`ok=False` 면 placeholder 유지(hot-loop 비차단, 실패측은 Item-8 conversion 이 안전 drop). `loop.py:ShadowConfig.balance_provider` → `SnapshotBuilder` 전달, `scripts/live_run.py::_wire_balance_provider` 가 3 파이프라인(attached/dual-kis/dual-bnb/_run_pipeline) 모두 주입. 이로써 라이브 inert 해소 — 실잔고 기준 정상 사이징.
- `reconnect.py` — 지수 backoff + uniform jitter 재연결 정책 (`backoff_delay`, `with_reconnect`). #133 hotfix 이후 `loop.py::producer` 가 `backoff_delay` 직접 사용하여 WS keepalive timeout 시 자동 재연결 (max 100 attempts, 1s → 60s).
- `price_cache.py` (#238 follow-up) — `LivePriceCache` thread-safe dict (Lock + `dict[upper(symbol), PriceSnapshot]`). `set_price(symbol, price, ts)` writer 는 `_run_mark_price_consumer` 가 매 mark-price update 마다 호출 (모든 USDT-perp ~수백 종목/초). `get_price(symbol) -> PriceSnapshot | None` reader 는 dashboard `/api/strategy_positions` 가 5초 polling 시 호출 → mark_price + pnl_pct overlay. 같은 인스턴스를 `ShadowConfig.live_price_cache` + `DashboardState.price_cache` 양쪽에 주입 (`scripts/live_run.py`). 단위 테스트 `tests/live/test_price_cache.py` (500-thread concurrent write 포함).
- `airborne_fire_consumer.py` (2026-06-11, 봉루프 decouple) — `AirborneFireConsumer` 백그라운드 task + `AirborneStrategySpec`. **루트원인**: consume 모드가 `history.jsonl` 발화를 직접 읽지만 *언제 평가하나* 가 트레이더 OHLCV 봉루프(`orchestrator.run_bar` → live-scanner on_bar)에 종속 — `run_bar` 은 `_universe_ohlcv`(트레이더 스냅샷) 종목만 평가하므로 universe refresh 랙으로 발화 종목이 그 봉 스냅샷에 없으면 영영 미평가 → 발화는 store 에 있는데 미진입(2026-06-11 "7시 롱 미매수": 22:00 UTC 12 발화 전부 미진입). **수정**: `sweep_once()` 가 `fire_store.load_since(now−freshness)` → 도착시각 게이트(`floor(fire_ts,1h).KST.hour ∈ kst_entry_hours`) + side 필터 + universe 필터 + freshness(now−fire_ts ≤ 600s, 재시작 backlog 재매수 차단) + BTC trend(long 차단, sweep 당 1회 캐시) → dedup 확인 → `orchestrator.dispatch_fire_entry(...)` → intent 면 route + dedup 마크. `run_loop(stop_event)` 은 interval_sec(15s)마다 `sweep_once`, PositionReconciler.run_loop 구조 mirror — 절대 raise 안 함(한 발화/한 sweep 예외 흡수). **dedup**: 기존 `logs/airborne_reentry/{ClassName}.json` 영속 파일을 전략 인스턴스(`_ensure_dedup_loaded`/`_fired_bar_ts`/`_persist_dedup`)로 공유, 키 값 = `str(bar_open)`(bar_open=floor(fire_ts,1h)−1h) 으로 on_bar consume 의 `closed_ts` 키와 정확히 일치 → 양 경로 동시가동해도 중복진입 0. 추가로 orchestrator `_live_entered` 가 (sid,sym) 1포지션 보장. **배선**(`loop.py`): `AIRBORNE_FIRE_CONSUMER=1` 시 `_start_airborne_fire_consumer` 가 등록된 `live-airborne*` 전략을 `_build_airborne_fire_specs` 로 introspect(kst_entry_hours/allowed_sides/get_universe/btc_trend_filter_enabled/instance) → consumer 구성 → run_loop background task(stop_event 공유). route_intents 는 consumer() 의 signal_emitted WAL→execute_intents 경로 재사용, equity_provider/btc_ohlcv_provider 는 consumer() 가 매 tick 갱신하는 `_live_snapshot_cache` 에서 read. on_bar consume 분기는 `_fire_consumer_active()` 로 hold(이중경로 차단). 미설정 시 byte-identical. **시간게이트 진입 스킵 알림(2026-06-14)**: ctor `notify: Callable[[str],None]|None`. 발화가 side 매칭 spec 의 hour 게이트를 전부 못 통과(=시간이 binding reason)하면 `sweep_once` 가 (hour,side)별 1건으로 집계해 "KST 14시 게이트 — 숏 진입 안 함 (N건): SYM…" 텔레그램 통지. fire 단위 dedup(`_skip_notified`, 같은 발화 매 sweep 재평가돼도 1회만) + universe/btc/dedup 로 막힌 건 `any_passed_hour=True` 라 오탐 제외. sync notify 는 `asyncio.to_thread` 로 호출(blocking 회피). `loop.py::_start_airborne_fire_consumer` 가 `observability.alerts.notify("info",…)` 로 배선. None 이면 no-op(테스트 기본). **롱 전용 짧은 freshness(2026-06-14, `long_freshness_sec` 기본 90s, env `AIRBORNE_LONG_FRESHNESS_SEC`)**: `side=="long"` 은 이 cap, 숏은 `freshness_sec`(600s). BTC 추세필터가 롱을 막으면 fire 가 store 에 남아 매 sweep 재평가되다 BTC 추세 풀릴 때 *묵은* fire_close 로 ~8분 뒤 진입(stale→NAKED, v0.6.65) → 롱은 봉마감 직후(≤~1.5분)에만 진입: BTC 상승추세 롱은 첫 sweep(~45s) 즉시 매수("정각 빠르게"), 그 안에 BTC 통과 못 하면 abandon. 숏은 기존 freshness 유지(재시작 backlog 보호). 상세: `docs/specs/airborne-fire-driven-consume.md`. 테스트 `tests/live/test_airborne_fire_consumer.py`(시간스킵 알림 5건 + 롱 freshness 3건 포함), `tests/portfolio/test_orchestrator_dispatch_fire_entry.py`.
- `fill_consumer.py` (Binance live-fill gap) — `binance/async_ws.stream_fills()` 의 **유일한 production consumer**. `broker_fill_to_order_filled_event(fill, *, symbol, side, strategy_id) -> WALEvent` (PaperBroker `order_filled` payload 스키마와 byte-호환 — `symbol/side/qty/fill_qty/fill_price/fees/fee_asset/client_order_id/broker_order_id/trade_id/server_ts/ts`, strategy_id 는 resolvable 시에만 키 추가) + `run_binance_fill_consumer(stream_factory, *, wal, position_store, stop_event, max_attempts, sleep)` (bounded-reconnect async task: `reconnect.backoff_delay` backoff, `(broker_order_id, trade_id)` 멱등 de-dupe, `StrategyPositionStore.resolve_order_context(coid)` 로 symbol/side/strategy 해석, 해석 불가 coid 도 WARNING 후 emit — 실자금 fill 절대 drop 안 함, `stop_event`/`CancelledError` 클린 종료, loop 절대 crash 안 함). WAL 의 기존 `observer` fan-out (timeline + position_store + pnl_aggregator) 그대로 재사용 — **parallel path 미생성**. 단위/E2E 테스트: `tests/live/test_binance_fill_consumer.py`, `tests/live/test_loop_binance_fill_wiring.py`.

Phase 1 추가 완료 (#80):
- `loop.py` — asyncio 이벤트 루프 (WindowsSelectorEventLoopPolicy), `run_shadow_loop`, `ShadowConfig`
- `executor.py` — `execute_intents` seam 함수 (kill-switch → 변환 → broker.place_order → metrics → post-only fallback)
- `post_only_fallback.py` (post-only Maker, 2~4단계) — GTX(post-only) LIMIT 미체결 fallback. EXPIRED→즉시 시장가, NEW→background task 로 cancel→시장가. partial fill / cancel-race 처리. 아래 전용 섹션 참조.
- `cs_basket_dispatcher.py` — universe-scan basket auto-orders (#218 follow-up, 2026-05-21). `orchestrator.run_bar` 는 `CrossSectionalAsyncStrategy` 의 Signal(symbol="CRYPTO_TOP30_BASKET") 을 받으면 size_to_qty 가 basket 가격을 못 찾아 OrderIntent 가 silent drop 됨. `BasketDispatcher` 가 별도 polling 으로 `strategy.latest_weights` → `portfolio.cs_rebalance_dispatch.dispatch_rebalance()` → 종목별 broker.place_order. weights signature cache 로 중복 dispatch 차단. **`loop.py:consumer()` 가 항상 wire** (2026-05-21 env gate 제거). graceful no-op — broker_mode 가 binance 아니거나 universe-scan strategy 가 등록 안 됐으면 latest_weights empty → skip. venue 별 lot_spec: USDT pair → `BINANCE_DEFAULT_LOT`, 그 외 → `KRX_LOT`. equity / prices 는 **`SnapshotBuilder.build_snapshot()` 의 dict 출력**에서 읽는다 — USDT pair → `snapshot["equity_usdt"]`, 그 외 → `snapshot["equity_krw"]`; prices → `snapshot["ohlcv_history"]` 의 종목별 마지막 close. **#324 회귀 박제**: 한때 `getattr(snapshot, "equity_usdt", ...)` 로 dict 에 attr 접근해 매 tick `reason=zero_equity` silent skip 됐다. 신규 헬퍼 `_snapshot_field(snap, key)` 가 dict/SimpleNamespace 둘 다 지원 (테스트 `TestDictSnapshotRuntimeShape`). **default broker 도 `binance-testnet-shadow` 로 변경 (2026-05-21)** — `python scripts/live_run.py` 한 줄로 cs-tsmom 자동발주 동작. KIS 모의 가동은 `--broker kis-paper-shadow` 명시 필요.

Phase 2 추가 완료 (#105):
- `executor.py` 확장 — `market_state: MarketState | None` (Architect note #1), `(WALWriteFailed, BrokerError)` catch (note #2 ack down-grade), `order_acked` WAL append (note #2), `tracking_sample` self-sim gate (note #3)
- `tracking_error.py` — `tracking_sample` WAL 집계 + `compute_tracking_error` + `emit_gauge(qta_paper_kis_tracking_error)` + `daily_report_row` (AC4)
- `strategy_returns_export.py` — `KisFillRecord`, `compute_daily_returns`, `export_to_orchestrator` → `register_strategy_returns` (CLAUDE.md 필수 의무)

#177 EXE 통합 변경:
- `loop.py:ShadowConfig` — `feed_mode` (auto/binance/kis/mock), `kis_client`, `wal_observer`, `mock_ticks`, `snapshot_builder_config` 추가.
- `loop.py:_select_feed` — symbol prefix 기반 KIS/Binance 자동 라우팅 + `--feed mock` smoke 지원.
- `loop.py:_load_orchestrator` — `on_metalabeler_missing="skip"` 사용 (모델 아티팩트 부재 시 해당 entry 만 skip).
- `loop.py:run_shadow_loop` — `SnapshotBuilder.warmup()` → consumer loop 가 `build_snapshot(tick)` 으로 ohlcv_history 풍부화. **(#216)** 진입 직후 `wait_for_session_fn(config.schedule)` 호출 (ProcessLock 보다 먼저). WAL 생성 직후 `emit_startup_events()` 로 `run_started` (+ schedule='krx' 일 때 `session_open`) heartbeat 기록. `wait_for_session_fn` 은 단위 테스트용 주입 가능 (default `wait_until_session_open`).
- `loop.py:emit_startup_events` (#216) — `run_started` (run_id/broker/feed/symbols/schedule/wal_path) + 선택적 `session_open` (date/kst_open) 첫 record 작성. 외부 모니터 (telegram-notifier WAL tail, report-cron WAL find) 가 daemon 부팅을 감지할 수 있게 함.
- **루프 silent-death 방어 (2026-06-06, "거래 시작 눌렀는데 좀 지나니까 stopped" 회귀 박제)** — `run_shadow_loop` 은 `asyncio.wait({producer, consumer}, FIRST_COMPLETED)` 구조라 둘 중 하나만 끝나도 전체 거래 루프가 종료된다 (→ reconciler/feed 전부 cancel). 셋이 겹쳐 "에러 로그 0줄로 조용히 멈춤" 이 발생했었다: ① consumer 의 per-tick 처리(`run_bar`/`execute_intents`/`position_risk_manager.evaluate`)가 try 없이 돌아 틱 하나 예외로 consumer task 가 죽음, ② producer 의 `async for` 가 예외 없이 끝나면(graceful WS close / finite feed) `break` 로 producer 가 종료, ③ finally 의 shutdown 루프가 `if not t.done()` 가드로 **먼저 끝난(=원인) task 의 예외를 회수 안 해** RunController 가 정상 return 으로 오인 → `STATUS_STOPPED`, 자동재시작 없음. 수정: ① consumer per-tick body 를 try/except 로 감싸 `consumer.tick_failed` 로깅 후 다음 틱 continue (한 틱 예외가 루프를 못 죽임), ② producer clean-exit 를 disconnect 와 동일한 backoff 재접속 경로로 통합 (graceful close 도 stop_event 전엔 재접속), ③ finally 에서 종료 task 의 예외를 `live loop ending — <task> raised ...` 로 surface. 회귀 테스트: `tests/test_loop.py::test_consumer_survives_tick_exception`, `::test_producer_reconnects_on_clean_feed_close`. **배경**: 신규 `bitget-demo` 기본 broker(`808c256`) 의 paper WS(`wspap.bitget.com`)가 90초마다 끊겨 트리거 빈도가 올라가며 드러남 — 구조적 결함은 그 전부터 잠재.
- **BitgetPublicFeed 재접속 데이터-0 + 심볼 balloon (2026-06-06 거래 stopped 사고 진짜 root-cause)** — `feed.py:BitgetPublicFeed`/`BitgetMarkPriceFeed` 의 `aclose()` 가 `self._closed=True` 로 설정하는데 `connect()` 가 **이를 다시 False 로 안 돌렸다**. loop 가 **같은 feed 인스턴스를 재사용**(`aclose()→connect()`)하므로 첫 끊김 이후 모든 재접속에서 `_iter` 가 첫 프레임(구독 ack)에서 `if self._closed: break` 로 **즉시 종료 → 틱 0개**. producer 는 (위 silent-death fix 로) "feed closed cleanly" 를 무한 재접속하다 attempt 100 소진하고 `producer exiting` → 거래 정지. 또한 `subscribe()` 가 blind `extend` 라 재접속마다 `config.symbols` 가 누적(운영 로그 430→1010)돼 구독 프레임 비대 + Bitget 종료 가속. 수정: `connect()` 에서 `self._closed=False` 리셋 + `subscribe()` dedupe(신규 심볼만 추가). 회귀 테스트: `tests/live/test_bitget_feeds.py::test_public_feed_connect_resets_closed_after_aclose`, `::test_public_feed_subscribe_dedupes_no_symbol_balloon`. (mark-price feed 는 매 재접속 새 인스턴스라 덜 치명적이나 동일 fix 적용.) 진단 단서: iter_start 후 ~58ms 만에 "closed cleanly" = 실제 네트워크 종료 아닌 즉시 break.
- `loop.py:ShadowConfig.schedule` (#216) — `Literal["krx", "always"]`, default `"always"`. `live_run --schedule=krx` 가 이 필드로 전달되어 게이트 활성화 (이전엔 argparse-only noop 이었던 #216 핵심 버그).
- `wal.py` 의 observer 콜백 (#181 머지로 도입) 을 `ShadowConfig.wal_observer` 로 wire-through.

#180 토글 와이어링:
- `loop.py:ShadowConfig.on_orchestrator_ready` — `_load_orchestrator` 직후 호출되는 콜백. `scripts/live_run.py` 가 이를 통해 `DashboardState.orchestrator` 에 라이브 인스턴스 주입 → `/api/strategies/{id}/toggle` 이 실제 orchestrator 의 enable/disable 을 호출. 콜백 예외는 swallow + log warn (loop 진행에 영향 X).

#192 strategy_id 태깅 + position_provider 와이어링:
- `executor.py` 의 `execute_intents()` 가 optional `position_store: StrategyPositionStore` 인자 수용 — 정상 ack 시 `register_order(client_order_id, strategy_id)` 호출.
- `executor.py` 가 WAL `order_acked` / `tracking_sample` 페이로드에 `strategy_id` 필드 직접 주입 (AC2). `types.py` 의 `OrderAckedPayload` / `TrackingSamplePayload` / `FillAnomalyPayload` 에 `strategy_id: str | None = None` 추가 (default None → schema v1 호환, 옛 페이로드는 fallback 으로 client_order_id prefix 파싱).
- `scripts/live_run.py:_run_pipeline` 에서 store 인스턴스 생성 → 부팅 시 `replay_from_wal(config.wal_path)` 로 복구 → `dashboard_state.position_provider = store.get_positions` → `wal_observer` 가 timeline_broker.publish + `store.ingest_fill_event` 둘 다 호출.

#194 라이브 PnL 와이어링:
- `pnl_aggregator.py` 신규. realized PnL 누적 + KST 09:00 business-date 기준 daily/monthly 분류. timer 없이 fill ts 검사 + property 호출 시 자동 reset 으로 24/7 정확.
- ~~WAL `order_filled` payload 의 strategy_id 는 PaperBroker 가 미주입~~ → **#238 review MEDIUM 수정**: `OrderRequest.strategy_id` (default None) 추가 → `intent_to_order_request` 가 `intent.strategy_id` 주입 → `PaperBroker._place_order` 가 `order_filled` payload 에 `req.strategy_id` 영속화 (non-None 일 때만 키 추가 → 레거시 byte-identical). aggregator/trade_history 는 payload `strategy_id` 우선 경로로 KIS-paper (`kis-paper-shadow`/`paper-only`) fill 을 정상 귀속 — replay-safe + cross-run 정확. `client_order_id` prefix 파싱 fallback (`_resolve_strategy`) 은 레거시 WAL 전용 (post-#238 coid 는 `:` 없는 strategy-opaque sha256 라 신규 fill 해석 불가). 테스트 `tests/live/test_paper_broker_strategy_attribution.py`.
- `scripts/live_run.py:_run_pipeline` 에서 aggregator 인스턴스 → `replay_from_wal` 로 부팅 복구 → `dashboard_state.pnl_aggregator` 주입 → `wal_observer` 가 timeline + position_store + pnl_aggregator 셋 다 호출.

#238 Bug B — client_order_id 전략 귀속 보존:
- **루트원인**: `executor._make_key` 가 `{strategy_id}:{symbol}:{ts_ms}:{idx}` 반환 → 실 long 전략(`live-breakout-with-atr-stop`=27자)은 36자 Binance `newClientOrderId` 한도 초과 → adapter 가 regex 실패로 `generate(strategy="fallback")` 의 opaque sha256 을 *대신 제출* → 돌아온 fill 의 coid 를 `_resolve_strategy` 가 매핑 불가(`:` prefix 없음) → 전략별 포지션/거래이력/PnL 귀속 silent loss.
- **수정**: `_make_key` 가 `client_id.generate(strategy=strategy_id, symbol, side=f"{side}:{idx}", ts_ms)` 로 Binance-valid 36자 결정론 coid 를 *생성 지점에서* 발급(전략명 cram 불가하므로 prefix 포기, idx 는 side 컴포넌트에 fold 해 배치 내 유일성 유지, 동일 ms 재시도 → 동일 coid = idempotent). `execute_intents` 가 `place_order` *이전에* 그 정확한 coid 로 `position_store.register_order` 호출 (이전엔 ack 이후 `ack.client_order_id` 로 등록). adapter 는 이미 valid coid 를 그대로 유지 → **불변식: 등록 coid == 제출 coid == 돌아온 fill coid**. StrategyPositionStore explicit map 이 prefix 파싱 없이 해석. REJECTED ack 에 등록돼도 무해(map 엔트리는 fill 없이 포지션 안 만듦). PnLAggregator/trade_history 는 payload `strategy_id` 우선 경로 그대로(로직 불변). 테스트 `tests/live/test_executor_coid_attribution.py`.

### Live Universe Scanner wire-up (#227 S3)

- `loop.py:ShadowConfig.position_risk_manager` (Optional `LivePositionRiskManager`) — set 시 consumer 가 매 tick `evaluate(symbol, last_price, ts)` 호출, 반환된 SELL `OrderIntent` 를 동일 broker/WAL/metrics 파이프라인으로 발주.
- `signal_emitted` WAL 이벤트 발행 (strategy intent 와 동일 schema, reason 에 `live_stop_loss` / `live_take_profit` / `live_trailing_stop` prefix).
- env-gated activation: `LIVE_SCANNER_ENABLED=1` (default OFF). `scripts/live_run.py::_run_pipeline` 가 환경변수 검사 후 `LivePositionRiskManager` 를 생성하고, `on_orchestrator_ready` 콜백에서 등록된 `LiveScannerMixin` 전략의 `stop_loss_pct` / `take_profit_pct` / `trailing_stop_pct` 클래스 속성을 정책으로 wire-up.
- 단위 테스트: `tests/live/test_loop_live_scanner_wiring.py` (evaluate 호출 / no-op 회귀 / SELL intent → WAL 라우팅).

### Multi-symbol mark-price feed (#238 follow-up — universe-wide stop/TP)

- **루트원인**: 기존 `BinancePublicFeed` 는 `config.symbols[0]` 하나의 aggTrade WS 만 구독 → consumer 가 `position_risk_manager.evaluate(tick.symbol, ...)` 를 그 단일 심볼에만 호출. universe-scanner (`live-scanner` paradigm) 가 매수한 28+ 종목의 mark price 가 system 에 안 들어옴 → ZEC/NEAR/SPK 등 +10% 상승해도 TP 평가 호출 자체가 일어나지 않아 영원히 보유.
- **수정**: `feed.py:BinanceMarkPriceFeed` 신규 — `!markPrice@arr@1s` combined stream 한 WS 로 **모든 USDT-perp** mark price 를 1Hz 로 push. yields `list[(symbol, Decimal mark_price, datetime utc)]` per push (non-mark events / non-array frames / 깨진 JSON 모두 silent drop). `loop.py:_run_mark_price_consumer` 가 매 batch 의 각 symbol 에 `position_risk_manager.evaluate(symbol, mark_price, ts)` 호출 + SELL intent 발생 시 동일 executor/WAL/metrics 파이프라인으로 라우팅 (mark price 기반 MarketState 자체 생성). reconnect/backoff 는 `reconnect.backoff_delay` 사용, evaluate 예외는 swallow + log + continue → trading loop crash 절대 없음.
- **활성화 조건**: `config.enable_mark_price_feed=True` (default True) AND `broker_mode=="binance-testnet-shadow"` AND `position_risk_manager` 셋. 셋 다 만족 시 `run_shadow_loop` 가 background task spawn (FIRST_COMPLETED 외부 — 자연 종료가 trading loop 끝내지 않음, finally clean cancel). 미만족 시 task 미생성 → paper/kis 경로 byte-identical.
- **가격 종류**: mark price (Binance funding/liquidation reference) — aggTrade last 보다 노이즈 작고, broker 측 자동 liquidation 기준과 일치 (우리 stop/TP 발사 시점 = broker 측 marginal 평가 시점).
- 단위 테스트: `tests/live/test_mark_price_feed.py` (parse / 비-array drop / malformed JSON skip / env URL normalisation) + `tests/live/test_mark_price_consumer.py` (전 symbol evaluate / SELL intent routing / evaluate exception swallow / stop_event 즉시 종료).

### Binance live-fill gap — stream_fills() production consumer

- **루트원인**: `src/brokers/binance/async_ws.py::stream_fills()` 가 production caller 0 (테스트만). 라이브 `binance-testnet-shadow` 경로에서 executor 는 `order_acked`(intent — Binance MARKET ack=status=NEW, price 없음)만 기록 → `order_filled` WAL 이벤트가 **단 한 번도** 발행 안 됨. `PnLAggregator`/`StrategyPositionStore`/`trade_history` 는 모두 `event_type != "order_filled"` 면 early-return → 라이브 Binance 경로 전체가 realized P&L 0 / 포지션 0 / 거래 0. 대시보드 매수/매도 이력·전략별 포지션·PnL 이 제출 INTENT(제출, price "—", intent qty)만 영구히 표시하고 실 체결(reduce_only-capped qty @ real price, FILLED)은 절대 안 보임.
- **수정**: `fill_consumer.py` 신규 (위 파일 목록 참조). `loop.py:ShadowConfig.position_store` (Optional `StrategyPositionStore`) 추가 — broker_mode=="binance-testnet-shadow" + `binance_adapter` + `position_store` 셋 다 있을 때만 `run_shadow_loop` 가 `run_binance_fill_consumer` 를 background task 로 spawn (FIRST_COMPLETED wait set **밖** — 자연 종료가 trading loop 를 끝내면 안 됨; finally 에서 clean cancel). `execute_intents` 양쪽 호출에 `position_store=config.position_store` 전달 → executor 가 place_order **이전에** `register_order` + `register_order_context(coid, symbol, side, strategy_id)` 등록 (post-#238 invariant: 등록 coid == 제출 coid == 돌아온 fill coid). 해석 불가 coid 도 strategy_id 키 없이 emit + WARNING (실자금 fill drop 금지, venue 합계는 정확 — #18 fail-safe). **`_resolve_fill_attribution(ctx, fill, store)` (2026-06-14)**: coid context 있으면 그대로(진입); 없으면(거래소 네이티브 TP/SL 청산 등 미등록 plan order) **fill 자체의 symbol/side** 사용 + `store.sole_holder_strategy(symbol)` 로 단독보유 전략 귀속(0명 수동/2명 다전략→None). binance·bitget consumer 공통. 이걸로 네이티브 TP/SL 청산이 `symbol=""` 로 기록·drop 되던 store 인플레이션(3~8×) 근본 차단. `BrokerFill` 에 symbol/side 필드 추가(기본 "", bitget `_parse_fill_from_order` 가 instId/side 채움; binance 등은 "" → 기존 동작 불변). 테스트 `tests/live/test_fill_attribution_native_tpsl.py`.
- **seam**: emit 된 `order_filled` 는 기존 `WAL.observer` fan-out (`scripts/live_run.py` 의 `_wal_observer` → timeline_broker.publish + position_store.ingest_fill_event + pnl_aggregator.ingest_fill_event + ops_counters.ingest + trade_history WAL replay) 을 그대로 통과. **parallel path 미생성** — paper 경로가 `order_filled` 에 쓰는 것과 동일 seam 재사용.
- **paper/kis byte-identical**: `binance_adapter` 미전달이면 fill-stream task 미생성 (PaperBroker 는 기존대로 자체 `order_filled` emit). `live_run.py` 3 binance-capable 파이프라인(`_run_pipeline_attached` / `_run_pipeline` / `_run_smoke_dual` 의 bnb branch)만 `config.position_store = position_store` 설정; smoke-dual KIS branch 는 미설정.
- **멱등성**: Binance `(broker_order_id, trade_id)` 키 de-dupe (reconnect 시 `async_ws` per-stream dedup set 이 재생성되므로 cross-reconnect resend 는 여기서 차단).
- **reconnect**: stream error → `reconnect.backoff_delay` (base=1s, cap=30s) bounded (default max 100 attempts) 재시도, loop crash 절대 안 함, `stop_event`/`CancelledError` clean 종료.
- 단위/E2E 테스트: `tests/live/test_binance_fill_consumer.py` (mapping / strategy 해석 / unresolvable→emit+warn / 멱등 / reconnect / cancel / place→ack→fill→PnL+position+trade_history E2E), `tests/live/test_loop_binance_fill_wiring.py` (paper no-op / binance mode stream 소비→order_filled).

### venue 실증 가시화 + KIS-inert 루트원인 (#238 follow-up)

- **루트원인**: `_wire_balance_provider` 가 파이프라인마다 *새* `AccountInfoProvider` 생성 → 자체 KIS auth+balance REST 호출. 라이브 KIS 데몬은 feed/warmup REST 와 경합해 EGW00201 rate-limit 으로 *간헐* 실패 → 구버전 `_inject_real_equity` 가 `equity_krw` 를 placeholder 로 *회귀* → Item-8 변환이 모든 KIS 주문 드롭 → "0 trades". standalone(무경합)에서는 같은 fetch 가 성공해 재현 안 됐음.
- **구조적 수정** (모든 경로 적용): `snapshot_builder.py` 가 venue별 `_last_good_equity` 보존. 한 번 real(>0) equity 를 본 venue 는 이후 *간헐* 실패에도 last-known-good 을 계속 overlay (거래 지속) + degraded 상태는 그대로 surface. **한 번도 못 본 venue 는 placeholder 유지** (fabricate 금지 → conversion 이 안전하게 drop). `last_equity_status[venue]={ok,reason,equity}` + ok 전이시에만 WARNING (per-tick flood 금지, 프로젝트 throttle 관례).
- **provider 재사용**: `_wire_balance_provider(cfg, existing=...)` 추가 — attached/smoke-dual 경로가 대시보드의 이미 warm 한 `state.account_info_provider` (15s 캐시, `/api/account/info` 폴링이 갱신) 를 재사용. `existing` 미지정 → 레거시 fresh-instance 와 byte-identical.
- **`loop.py:ShadowConfig.on_snapshot_builder_ready`** — `SnapshotBuilder` 생성 직후 1회 호출 (orchestrator ready 콜백과 동일 패턴, 예외는 swallow+warn). live_run.py 가 `DashboardState.snapshot_builder` 를 wire → `/api/venue_equity_status` 노출.
- **Issue 2 (영구 이력)**: `_run_pipeline`/smoke-dual 이 `dashboard_state.log_dir = wal_path.parent.parent` 항상 set, `_serve()`(standalone)는 `Path("logs/live")` seed. 이전엔 미설정이라 매 run 이력 소멸.
- 단위 테스트: `tests/live/test_snapshot_builder_equity_status.py` (status/throttle/last-known-good/실 KIS payload), `tests/test_live_run_equity_visibility_wiring.py` (ready 콜백/provider 재사용/log_dir seam), `tests/test_dashboard_history_permanence.py` (cross-run 합산/fallback/endpoint).

### balance fetch off-loop (prior-review MEDIUM #3)

- **문제**: `build_snapshot(tick)` 는 라이브 consumer 코루틴(`loop.py`)에서 *동기* 호출 → 구버전 `_inject_real_equity` 가 `provider.fetch()` 를 inline 실행. 15s 캐시 *miss* 시 이벤트루프 스레드가 KIS+Binance REST 에 block (=#18 KIS 경합 지점).
- **수정** (WHERE 만 이동, 적용 equity 값은 byte-identical): `SnapshotBuilder.refresh_balance()` 추가 — `provider.fetch()` 를 호출하는 off-loop 진입점. `loop.py` consumer 가 동기 `build_snapshot` *직전* `await asyncio.to_thread(snapshot_builder.refresh_balance)` 로 캐시를 pre-warm (cache-miss REST 만 스레드로). `_inject_real_equity` 는 `provider.peek()`(REST 없는 non-blocking 캐시 read)로만 읽음 → 이벤트루프 스레드는 절대 REST block 안 함. `peek()` 미구현 provider/테스트 fake 는 `fetch()` fallback (pre-#3 계약 보존, `prov.calls==2` 회귀 유지).
- **`balance_provider is None` (기본/레거시/테스트)**: `loop.py` 가 `config.balance_provider is not None` 일 때만 thread-hop → await point 추가 없음 → producer/consumer 스케줄링 byte-identical (mock-feed 결정성 회귀 0). last-known-good + `last_equity_status` 의미 불변.
- 단위 테스트: `tests/live/test_snapshot_builder_offloop_balance.py` (peek 사용/이벤트루프 스레드에서 fetch 미실행/refresh 전 placeholder 안전/None byte-identical/last-known-good/fetch fallback).

### post-only Maker 진입 (post-only-maker-entry, 2~4단계)

- **목적**: 진입(BUY)을 Taker 시장가 → Maker GTX(post-only) LIMIT 으로 전환 — 진입 수수료 0.045%→0.018%(진입 한쪽 60%↓). 청산(SELL)은 손절 즉시성 우선으로 market 유지 → 왕복 수수료 0.090%→0.063%(**전체 약 30% 절감**, 일 −104→−70대 USDT). 배경 — Binance testnet 실측 REALIZED_PNL +94.66 / COMMISSION −104.52 = NET 순손실. (TP 청산까지 post-only 화하면 추가 절감 — 후속, draft 참조.)
- **발주 경로** (`executor.py`): `OrderIntent.entry_order_type == "post_only"` 이고 `ref_price` 가 있으면 `_build_order_request` 가 maker 보장 limit 가격(`_post_only_limit_price`: buy `ref×0.9995`, sell `ref×1.0005`)을 산출해 `order_type=LIMIT, tif=GTX` 로 변환. tick-size 정렬은 broker adapter(`AsyncBinanceFuturesAdapter._quantize_price_to_tick`)가 담당. ref_price 누락 시 안전하게 MARKET 강등.
- **fallback** (`post_only_fallback.py`): GTX 는 즉시 체결 안 됨. `execute_intents` step 7 이 `ack.status` 분기 —
  - **EXPIRED** (taker 가 될 주문이라 거래소 거부) → 즉시 전량 시장가 재발주 (inline, 대기 없음).
  - **NEW/PARTIALLY_FILLED** → `schedule_post_only_fallback` 가 background task 예약 (tick loop **비블로킹**). `POST_ONLY_FALLBACK_SEC`(30s) 후 `get_order` → cancel → 잔량 시장가.
  - **partial fill**: cancel 후 `executedQty`(`OrderAck.filled_qty`)를 읽어 잔량(origQty − executedQty)만 재발주. dust 잔량은 adapter minQty floor 가 자연 거부.
  - **cancel-race (gap D)**: cancel 실패 시 re-GET. FILLED 면 재발주 금지(중복 차단). cancel 실패 + 주문 활성이면 보수적 abort (`PositionReconciler` 정합에 위임).
- **`_live_entered` 박제 회피 (4단계)**: 완전 미체결(체결 0) + 시장가 재발주도 REJECTED 면 `on_entry_unfilled` 콜백 → `loop.py` 가 `orchestrator.sync_live_entered(sid, sym, 0)` 으로 진입 기록 해제. 부분 체결분이 있으면 포지션 존재 → 해제 안 함.
- **리스크 우회 금지**: 시장가 재발주는 `execute_intents` 를 그대로 재사용 (KillSwitch/sizing/WAL/position_store 동일 파이프라인). 재발주 intent 는 `entry_order_type="market"` → 재귀 fallback 없음.
- **종료 처리**: `run_shadow_loop` 종료 시 `cancel_pending_fallbacks()` 로 in-flight fallback task 취소 (미취소 시 종료 후 시장가 발사 위험).
- **관측**: `post_only_fallback` WAL 이벤트 — `outcome` ∈ {filled_maker, filled_during_cancel, resubmitted_market, total_miss, cancel_failed_abort}. 5단계(전략별 토글) rollout 시 미체결률 모니터링 근거.
- **5단계 미완** (별도 PR): strategy 에 `entry_order_type` 속성 추가 + production.yaml 적용. orchestrator 는 이미 `getattr(strat, "entry_order_type", "market")` 로 읽어 stamp.
- 테스트: `tests/live/test_post_only_fallback.py`, `tests/portfolio/test_orchestrator_post_only_stamp.py`.

## 불변식

- **WAL-first**: 모든 주문/체결 이벤트는 append-only JSONL에 먼저 쓰고 메모리 반영. 쓰기 실패 시 주문 거부 + kill-switch trip.
- **Decimal 경계 단일 지점**: float→Decimal 변환은 `src/live/conversion.py::intent_to_order_request` 한 곳에서만 발생. `Decimal(float)` 직접 호출 금지.
- **Time source = 시스템 UTC**: WAL `ts`, 메트릭 ts 모두 `datetime.now(timezone.utc).isoformat()`. WS 서버 시간은 별도 `server_ts` 필드로 구분.
- **Windows SelectorEventLoop 정책**: `loop.py` 최상단에서 `WindowsSelectorEventLoopPolicy` 설정 (Phase C). ProactorEventLoop 금지.
- **단일 프로세스 락** (FMEA F9): `process_lock.py` 기반, 중복 실행 방지.

## 2026-06-07 #380 — leverage 강제 (QTA_TARGET_LEVERAGE)

- `executor.py` — `_target_leverage()` 가 `QTA_TARGET_LEVERAGE` env(양의 int)를 읽어, 설정 시 발주 직전 `broker.ensure_leverage_target(symbol, N)` 으로 leverage 를 *강제*(현재값 override). 미설정/0/비정수면 기존 `ensure_leverage_minimum`(1x 안전망) 경로 byte-identical. bitget 어댑터만 `ensure_leverage_target` 보유(KIS/Paper getattr graceful skip). 사고 배경: leverage 1x 방치로 short-whitelist 가 5000$ 중 70% margin 소진. 테스트 `tests/live/test_executor_order_rejected_wal.py` (target 강제/fallback/invalid/실패시 발주).

## 참조

- `docs/work/active/000080-paper-broker/01_plan.md` — Phase A~E 전체 구현 계획
- [[29-paper-to-live-protocol]] — Phase 1→2 승격 기준
- [[09-system-components]] — 시스템 컴포넌트 전체 맵
