# src/backtest/strategies/

## 목적
`Strategy` protocol 구현체. 각 전략은 `on_init()`과 `on_bar()`를 구현.

## 구조
- `momo_btc_v2.py` — BTC 15m 모멘텀 전략 (RSI divergence 진입, long-only MVP). `sizing_mode: "full"|"half-kelly"|"vol-target"` 옵션으로 진입 크기 결정 방식 선택 가능 (기본 `"full"`, #69).

## 규칙
- `src/backtest/protocol.py`의 `Strategy` protocol 준수 필수
- MVP는 long-only: bearish divergence는 현금 전환 (숏 없음)
- LLM 호출 금지 (레포 불변식 #6) — 진입 크기 계산도 `src/risk/sizing.py` 순수 함수만 경유
- 진입 크기 계산이 필요한 전략은 `src/risk/sizing.py` 의 함수 조합으로만 결정 (μ̂/σ̂ 추정 포함 전부 결정적 코드)

## 관련 문서
- `docs/specs/strategies/momo-btc-v2.md`
- `docs/specs/position-sizing.md` (#69)
- 이슈 #67, #69
