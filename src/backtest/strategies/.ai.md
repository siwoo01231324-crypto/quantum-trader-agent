# src/backtest/strategies/

## 목적
`Strategy` protocol 구현체. 각 전략은 `on_init()`과 `on_bar()`를 구현.

## 구조
- `momo_btc_v2.py` — BTC 15m 모멘텀 전략 (RSI divergence 진입, long-only MVP). `sizing_mode: "full"|"half-kelly"|"vol-target"` 옵션으로 진입 크기 결정 방식 선택 가능 (기본 `"full"`, #69).
- `meanrev_pairs.py` — ETHBTC 1h 로그가격 z-score 평균회귀 전략 (AsyncStrategy). zscore 팩터 + vol_target 사이징. Bar boundary: 매 정시 UTC. (#79)
- `momo_vol_filtered.py` — BTCUSDT 4h MACD 모멘텀 + 변동성 필터 전략 (AsyncStrategy). realized_vol < vol_ceiling 조건 필터. (#79)
- `breakout_donchian.py` — KOSPI200 1d Donchian 채널 돌파 전략 (AsyncStrategy). KRX 일봉, top-10 ATR 정규화 선택, 비대칭 KRX 비용 모델. Bar boundary: KRX 15:30 KST. (#79)
- `momo_kis_v1.py` — KIS KRX 15m 모멘텀 (RSI divergence 진입, 단일종목 005930 기본, KRX 거래시간 gate + is_krx_holiday 2중 안전망). (#96)

## 규칙
- `src/backtest/protocol.py`의 `Strategy` protocol 준수 필수
- MVP는 long-only: bearish divergence는 현금 전환 (숏 없음)
- LLM 호출 금지 (레포 불변식 #6) — 진입 크기 계산도 `src/risk/sizing.py` 순수 함수만 경유
- 진입 크기 계산이 필요한 전략은 `src/risk/sizing.py` 의 함수 조합으로만 결정 (μ̂/σ̂ 추정 포함 전부 결정적 코드)

## 리스크 연동 (필수 — #70 이후)

모든 전략은 일수익률 시계열을 **포트폴리오 리스크 모듈로 공급** 해야 한다. 이 규칙은 선택이 아니라 **구현 계약** 이다.

### 어떻게
```python
from portfolio import StrategyOrchestrator
orch.register_strategy_returns("<strategy_id>", daily_return_series)
orch.refresh_portfolio_risk()
```

- `daily_return_series: pd.Series` — index=날짜, 값=그날 실현 수익률
- `strategy_id` = 전략 파일명 snake_case (예: `momo_btc_v2`)

### 왜
- `src/risk/portfolio.py` 의 LW shrinkage Σ / Meucci ENB / Historical CVaR 는 T×N 수익률 매트릭스를 입력으로 받음 ([[19-portfolio-risk]] §2.2, §3.1, §4.1).
- 전략이 수익률을 공급하지 않으면 `portfolio_risk is None` → 리스크 평가기가 **항상 ALLOW 경로** → 리스크 관리 무력화.
- 본 프로젝트는 **단일 evaluator 경로** (`risk.evaluate`) 유지 — 전략마다 자기 리스크 룰을 재구현하면 `rule_id` 라벨 공간 오염.

### 준수 체크리스트 (새 전략 추가 PR 기대치)
- [ ] `daily_return_series` 산출 경로 명시 (backtest runner 또는 라이브 loop)
- [ ] `strategy_id` = 전략 파일명 snake_case
- [ ] `docs/specs/strategies/<strategy_id>.md` 에 "리스크 연동: `register_strategy_returns(...)`" 기재
- [ ] 단위 테스트 1건 — 본 전략 수익률을 흘려 report 생성 확인 (`tests/test_portfolio_orchestrator.py` 또는 전략-전용 테스트)

### 예외 (사전 승인 필요)
리스크 연동을 생략하려면 전략 spec 의 **운영 규칙** 섹션에 사유를 명시하고 PR 리뷰에서 승인. 일반적으로 생략 불가.

## 메타라벨링 훅 추가됨 (#85)

`momo_btc_v2.py` 는 LightGBM 2차 메타라벨러 옵션을 지원한다 (opt-in, 기본 disabled).

### 시그니처 확장 불변식
- `metalabeler=None` 이 기본값 — 기존 호출 경로의 시그니처·동작 변경 없음, 회귀 없음
- `metalabeler_threshold: float = 0.5` — 임계값도 opt-in

### 동작
```python
strategy = MomoBtcV2(metalabeler=fitted_ml, metalabeler_threshold=0.5)
# metalabeler=None (기본) → bypass, 기존 동작 보존
```

- `p_take >= threshold` → 신호 통과, `Signal.win_probability = p_take`
- `p_take < threshold`  → `Signal(action="hold", reason="metalabeler_reject", win_probability=p_take)`

### 전략 시그니처 확장 규칙
새 opt-in 파라미터를 전략 `__init__` 에 추가할 때 **반드시 기본값을 기존 동작과 동일하게** 유지해야 한다. 기존 테스트를 수정하지 않고 통과해야 회귀 없음이 보장된다.

### 관련 파일
- `src/ml/meta_labeler.py` — MetaLabeler API
- `tests/backtest/test_momo_btc_v2_metalabeler.py` — 통합 테스트 4건
- `docs/background/35-meta-labeling-lopez-de-prado.md` — 이론 배경

## 관련 문서
- `docs/specs/strategies/momo-btc-v2.md`
- `docs/specs/position-sizing.md` (#69)
- `src/portfolio/.ai.md` — 오케스트레이터 계약
- `src/risk/.ai.md` — 리스크 모듈 공개 API
- 이슈 #67 (초기 momo), #69 (사이징), #70 (리스크 모듈), #78 (멀티 전략 async 오케), #79 (카탈로그 확장), #85 (메타라벨링)
