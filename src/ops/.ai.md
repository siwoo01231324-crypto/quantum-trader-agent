# src/ops — 운영(Ops) 레이어

## 목적
Kill switch / DR 게이트와 자동 트리거, 수동 CLI 를 제공한다.

## 구조
- `kill_switch.py` — `KillSwitch` 게이트 (`allow_order` / `trip` / `release`)
- `triggers.py` — 자동 트리거 4종:
  - `DrawdownTrigger`: peak tracking 기반 DD 감시 (Phase D-1 강화). `starting_equity` 필드 보존(backward compat), 첫 update 시 peak 초기화.
  - `ApiErrorTrigger`: consecutive 카운트 기반 (legacy, 호환성 보존).
  - `ApiErrorRateTrigger`: sliding window rate 기반 신규 (5분, 임계 5%, 최소 표본 20건).
  - `FillAnomalyTrigger`: 1초 5건 burst 감지 + `dump_path` JSONL 덤프 옵션 (Phase D-1 추가).
- `cli.py` — `python -m src.ops.cli {kill|release|status}`

## 규칙
1. 모든 신규 주문은 `KillSwitch.assert_allow_order()` 또는 `allow_order()` 통과 필수.
2. 청산 주문은 `liquidation=True` 로 게이트 통과(화이트리스트).
3. 자동 트리거는 `KillSwitch.trip(source="auto:*")` 로 통일된 경로를 사용.
4. 운영 상태 영속화는 `KILL_STATE_PATH` 환경변수 또는 기본 `.ops/kill_state.json`.

## 관련 문서
- 명세: `docs/specs/kill-switch-dr.md`
- 런북: `docs/runbooks/kill-switch-runbook.md`
- 테스트: `tests/test_kill_switch.py` (dry-run)
