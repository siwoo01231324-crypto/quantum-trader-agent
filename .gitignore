# 금지 파일 형식 (레포 규칙)
*.pdf
*.csv
*.pkl
*.parquet

# 데이터 레이크 (로컬 Parquet 저장소)
lake/

# ML 모델 아티팩트 — 실 아티팩트는 별도 모델 레지스트리/CI 로 관리 (#85, #94, #95)
models/

# 환경 변수
.env
.env.*
!.env.example

# 의존성
node_modules/
.venv/
__pycache__/
*.pyc
*.egg-info/

# 빌드 결과물
dist/
build/
.next/
out/

# 테스트 결과물
test-results/
playwright-report/
coverage/
.coverage

# 워크트리 (git이 자동 관리)
.worktree/

# OS
.DS_Store
Thumbs.db

# Claude Code 로컬 설정
.claude/settings.local.json
.claude/scheduled_tasks.lock

# OMC (oh-my-claudecode)
.omc/

# IDE
.idea/
*.swp

# 중복 경로 방지 — #84 작업 중 발견된 오래된 백업 흔적 (tests/fixtures/ 를 통째로 복사한 잔해).
# 과거 한 번의 수동 실수가 mtime 보존 복사(cp -rp)로 untracked 상태로 남아있던 케이스.
# 재발 시 scripts/check_invariants.py 에서 FAIL 처리됨.
tests/fixtures/fixtures/

# Obsidian 볼트 (개인 설정만 제외, 공통 설정은 커밋)
docs/.obsidian/workspace.json
docs/.obsidian/workspace-mobile.json
docs/.obsidian/cache/
docs/.obsidian/plugins/
docs/.trash/

# GraphDB 런타임 데이터
infra/graphdb/data/
infra/graphdb/logs/
