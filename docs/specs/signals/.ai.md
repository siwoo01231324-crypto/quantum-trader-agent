# docs/specs/signals/

## 목적
진입·청산 신호 정의 노트. 각 파일 = 하나의 Signal 노트 (`type: signal`).

## 스키마
`docs/schemas/note-schemas.md` 의 **Signal** 타입.

## 파일명 규칙
- `{id}.md` (id 는 kebab-case)

## 관련
- ML 모델 id 를 `source_model` 로 연결하면 `qta:derivedFromModel` 관계가 온톨로지에 기록됨
- #47
