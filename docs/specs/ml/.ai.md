# docs/specs/ml/ — ML 모델 스펙 디렉토리

## 목적
`src/ml/` 모듈에 대한 기술 스펙 문서 보관. 메타라벨링 레이어 설계, 인터페이스, CV 프로토콜, 아티팩트 레이아웃을 정의한다.

## 파일 목록
| 파일 | 내용 |
|------|------|
| `meta-labeling.md` | 메타라벨링 레이어 전체 스펙 (AC5) |

## 프론트매터 규칙
- `type: spec-architecture` 재사용 — `docs/schemas/note-schemas.md` 에 `ml-model` 타입 없음
- `id` 는 파일명과 일치 필수 (CLAUDE.md 불변식 #2)
- 새 스펙 추가 시 이 파일 목록 업데이트
