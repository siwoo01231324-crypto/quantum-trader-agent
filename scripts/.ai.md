# scripts/

## 목적
CI 및 로컬에서 실행되는 유틸리티 스크립트를 보관한다.

## 파일
| 파일 | 역할 |
|------|------|
| `check_invariants.py` | 아키텍처 불변식 검증 (위반 시 CI 차단, `--strict` 기본 CI) |
| `check_strategy_completeness.py` | 전략 8 레이어 등록 완전성 검증 (spec / 코드 / 테스트 / production.yaml / live-scanner stop·TP / universe-scan module·pin-date / 5y backtest 결과 / orphan). `check_invariants.py` 다음 단계 운영 invariant. `--strict` 시 error 1건이라도 있으면 exit 1. CI: `.github/workflows/strategy-completeness-check.yml` (v1 warn 모드). |
| `check_forbidden_files.py` | 금지 파일 커밋 여부 검사 |
| `ontology_sync.py` | 프론트매터 → `docs/ontology/instances.ttl` 동기화 (#47) |
| `migrate_frontmatter.py` | `docs/**/*.md` 프론트매터 일괄 주입 (경로 기반 type 추론, idempotent) (#52) |
| `shacl_validate.py` | SHACL 제약 기반 도메인 규칙 검증 (pyshacl, `--strict` 시 fail) (#54) |
| `graphdb_client.py` | GraphDB REST 공용 HTTP 헬퍼 (wait_for_ready, create_repo, upload_ttl, sparql_update) (#55) |
| `graphdb_bootstrap.py` | GraphDB `qta` 리포지토리 생성 + T-Box (`trading.ttl`) 로드 (#55) |

| `fetch_candles.py` | Binance REST API → OHLCV Parquet 데이터 수집 CLI (#67) |
| `run_backtest.py` | 백테스트 전략 실행 CLI — Sharpe/MDD 출력, 프론트매터 업데이트, doc_agent 초안 생성 (#67) |
| `compare_momo_btc_v2_sizing.py` | momo-btc-v2 `full` · `half-kelly` · `vol-target` 3 모드 동시 백테스트 → Sharpe·MDD·total_return·trades JSON 저장 (#69) |
| `train_metalabeler_btc.py` | 메타라벨러 단일 학습 thin CLI — `src/ml/retrain_pipeline` import 후 argparse + main (#85) |
| `retrain_metalabeler.py` | 메타라벨러 주간 재학습 오케스트레이터 — 드리프트 감지·알림·latest.json 갱신·로그 작성 (#95) |
| `shadow_run.py` | Phase 1 Shadow Live Loop CLI 진입점 — BinancePublicFeed(aggTrade WS) + WAL + PaperBroker (#80) |
| `shadow_report.py` | WAL 파싱 + 일별 PnL → daily return → Sharpe 비교 4조건 + Strategy returns export + Exit Criteria 자동 검증 (#80) |
| `kis_paper_smoke.py` | KIS 모의계좌 smoke test (CI nightly + 수동) — 인증/주문/체결/포지션 조회 정상 확인. paper=True 시 `HANTOO_FAKE_*` 변수 사용 (#105, #133 변수명 정정) |
| `live_run.py` | Phase 2 KIS 모의계좌 Live Loop CLI 진입점 — --broker/--symbols/--duration/--auto-fallback/--schedule. broker_mode 가 kis-paper* 면 KIS adapter 직접 생성하여 run_shadow_loop 에 전달 (#105). 인자 없이 실행 시 dashboard 자동 기동 + 자동 브라우저 (`_run_dashboard_only_mode`), 정상 인자 시 거래 + dashboard + 자동 브라우저 (`--no-browser` 로 비활성), 시작 배너 ASCII art, PyInstaller bundle (`sys._MEIPASS`) 인식 (#182). `--check-bundle` 로 CI smoke 검증 (#215 — 전략 카탈로그/production.yaml 번들 누락 자동 검출). `_autoload_dotenv()` 가 EXE/cwd 부모 디렉토리를 walk-up 하며 `.env` 자동 탐색 (#215). **2-모드 대시보드 버튼(2026-06-30)**: `_resolve_run_mode(mode)` 가 거래 모드→config·broker·env 매핑(순수 함수) — `_build_pipeline_factory._factory` 가 `params["mode"]` 로 분기. `"swing"`=swing_mainnet.yaml+bitget-mainnet+SWING_CHANNEL_SWEEP/SWING_EVAL_TIMER_SEC, 기본=production.yaml. 인자없이 기동(`_run_dashboard_only_mode`)한 대시보드의 [에어본]/[스윙] 두 시작버튼이 호출 → CLI/taskkill 없이 토글, RunController 상호배제로 동시가동 차단. 테스트 `tests/test_run_controller.py` (TestRunModeResolution/MutualExclusion). |
| `live_report.py` | WAL → AC 6개 자동 검증 + R1~R5 halt trigger 평가 → Markdown 리포트 (#105) |
| `leverage_scenario.py` | 레버리지 시나리오 계산 — `apply_leverage` + `compute_leverage_metrics` (사후 곱하기 근사 + funding cost + monthly 10% hit ratio) (#119) |
| `measure_strategy_catalog_3y.py` | 카탈로그 5종 3년 윈도우 측정 — `measure_strategy_catalog.py` 의 START=2023-04-27, END=2026-04-27 사본 (#119) |
| `bench_iranyi_variants.py` | Iranyi VWMA #99 8-variant 사전등록 factorial bench — PurgedKFold + DSR + PBO + sha256 무결성 + DATA_UNAVAILABLE 분기 + smoke 모드. 5년 SOP run 결과는 `docs/work/done/000099-iranyi-vwma-research/bench_output.json` (#99) |
| `fetch_alt_universe_ohlcv.py` | Binance USDT-M perp top-10 alt OHLCV fetcher. `--dry-run` 으로 API 호출 없이 fetch 범위 출력. 출력: `lake/ohlcv/freq=5m/year=YYYY/month=MM/symbol=XXX/part-0.parquet`. **(#185 에서 BTC/ETH/SOL/BNB/XRP/ADA/AVAX/DOGE/LINK/ATOM 5m 5년 270MB 수집 완료, 다른 alt 전략에 재사용 가능)** |
| `bench_iranyi_full_stack.py` | Iranyi 12-Rules #185+#206 D0~D9 사전등록 full-stack bench — D0-D5(4h BTC), D6-D9(5m multi-asset). sha256(`8405cf460...`)+git_commit witness, cv_split_hash, cv_params, gate_params 모두 JSON 출력. multi_tf gate / turning_point / multi-asset routing 실 연결 (#206). 메타라벨러 wiring 만 stub. **🚫 Iranyi 가설 negative result 폐기 — `docs/work/done/000185-*` + `000206-*` 02_implementation.md 참조. multi-asset bench 인프라 (`_run_variant_multi_asset`, `_build_cross_asset_rs_filter`) 는 다른 cross-sectional 전략 평가에 재사용 가능.** |
| `cron_loop.sh` | Docker container 내부 sleep-loop — 매일 KST 16:00 발화 → live_report.py 실행 → telegram_alert.py 요약 발송 (#133). #152 에서 LIVE > QTA > legacy alias export 추가 (모든 알림 LIVE 봇 단일 채널). |
| `telegram_alert.py` | Telegram bot 알림 utility (단방향 outgoing) — `--watch DIR` (WAL tail critical event), `--report PATH` (일일 리포트 요약), `--test` (ping). 토큰 변수 우선순위: `TELEGRAM_LIVE_BOT_TOKEN/CHAT_ID` (1순위, 운영 표준) > `TELEGRAM_QTA_BOT_TOKEN/CHAT_ID` (2순위) > legacy `TELEGRAM_BOT_TOKEN/CHAT_ID` (#152). 토큰 미설정 시 skip (daemon 정상 작동) (#133, #152). |
| `telegram_control.py` | Telegram 양방향 제어 봇 (#126) — `getUpdates` long-poll → `/kill /release /status /policy /today /positions /fills /account /help` 명령 → localhost dashboard REST API 호출 → 응답. chat_id 화이트리스트 + WAL `command_received` 감사 로그. `/release` 는 60초 내 2-step 확인 (실수 방지). 토큰 변수 우선순위: `TELEGRAM_LIVE_BOT_TOKEN/CHAT_ID` (1순위) > `TELEGRAM_QTA_*` > legacy (#216 — telegram_alert.py 와 동일 fallback chain). docker-compose.live.yml 의 `telegram-control` service 가 `network_mode: service:live-daemon` 으로 dashboard 의 127.0.0.1:8000 에 도달 (#216 운영 wiring, 5번째 컨테이너). 거래 현황 명령 (`/today` 오늘 PnL + 전략별 손익, `/positions` 종목별 보유, `/fills [N]` 최근 N개 체결, `/account` 잔고+가용현금) 는 `/api/pnl /api/strategies /api/shadow_runs /api/account/info` 호출 (#221). |
| `local_setup_windows.md` | Windows + Docker Desktop 24/7 운영 셋업 가이드 — 절전 끄기, Update 정책, Docker 자원 할당, 첫 빌드/실행, 일일 운영 루틴 (#133) |
| `bench_swing_strategies.py` | #99 iter2 스윙 전략 S1-S5 + S6(앙상블) 사전등록 bench — 1m→4h resample + PurgedKFold + DSR + PBO + sha256 무결성 + S4 DATA_UNAVAILABLE + smoke 모드. 결과: `docs/work/done/swing-strategy-best-return/bench_output.json` (#172) |
| `bench_swing_iter3.py` | #99 iter3 S2 변형(S2a/S2b/S2c) + S4(funding) + S4a + S6v2 사전등록 bench. 결과: `bench_output_iter3.json` (#172) |
| `bench_swing_iter4.py` | #99 iter4 variant 축소(W1-W3) + S2c x S4 composite + PBO 가설 검증. 결과: `bench_output_iter4.json` (#172) |
| `bench_swing_iter5_stability.py` | #99 iter5 W1(S2c) parameter stability grid — 81 combo (entry/exit/vol_target/vol_lookback) Sharpe/MDD/mhr 분포 + robustness CV 판정. 결과: `bench_output_iter5_grid.json` (#172) |
| `fetch_funding_rates.py` | Binance Futures funding rate fetcher — `/fapi/v1/fundingRate` API → parquet (lake/funding_rate/) (#172) |
| `bench_metalabeler_btc.py` | momo-btc-v2 메타라벨러 ON/OFF Sharpe·Sortino·MDD 비교 bench — AC4 임계 자동 판정 (#85) |
| `bench_metalabeler_kis.py` | momo-kis-v1 메타라벨러 KRX bench — CV/PR-AUC + equity-curve Sharpe/Sortino/MDD/DSR + DSR 자동 판정 (`--metalabeler-threshold`, `--periods-per-year`, `--multi-symbol`) (#97 v5, #154). `--manifest-dir` 로 manifest.json 영속 저장 (default `models/momo-kis-v1[-pooled]/<UTC ts>/`) — `cross_asset_compare.py` 가 이 경로를 default 로 읽음 (#155). |
| `cross_asset_compare.py` | BTC + KIS manifest 로드 → 5섹션 비교 리포트 자동 생성 (`docs/work/active/000097-kis-cross-validation/02_implementation.md`). default `--kis-model-dir = models/momo-kis-v1-pooled` 가 bench `--manifest-dir` default 와 일치 (#97, #155). |
| `cron_fetch_kis_daily.py` | KIS 분봉 매일 1회 적재 cron 진입점 (#152). `--n-pool 30 --interval 1m` default. env 변수: `HANTOO_FAKE_API_KEY`, `HANTOO_FAKE_SECRET_API_KEY`, `HANTOO_CREDIT_NUMBER` (`12345678-01` 형식, dash 분할) 우선 + 레거시 `KIS_APP_KEY` fallback (`resolve_kis_credentials` 헬퍼). |
| `kis_1m_fetch_loop.sh` | #152 KIS 1m fetch + 주간 누적 summary 컨테이너 entrypoint. `cron_loop.sh` 패턴 차용 — 평일 KST `FETCH_HOUR_KST` 에 fetch + 매 결과 Telegram, `WEEKLY_DOW` 요일에 `kis_lake_monitor.py` 로 누적 진척도 Telegram 발송. |
| `bitget_account_reconcile.py` | Bitget 계좌 잔고 검산 (read-only, 불변식 #6). `reconcile(date_kst)` → `/api/v2/mix/account/bill` running balance 페이지네이션 → 전일 종료(open)→당일 종료(close) Δ, 거래 flow, fees, 입출금/이체 격리(`no_external_flow`), businessType 분포 반환. 일일 리포트(`docs/journal/`) "잔고 검산" 섹션 + `export_journal_for_date.py` 의 `account_reconciliation` JSON 필드용. 일일손익(history-position netProfit)이 실제 잔고흐름과 앞뒤로 맞는지 tie-out — WAL reconstruct 금지([[feedback-journal-pnl-use-exchange-ledger]])의 교차검증 짝. `.env`(BITGET_*, mainnet) autoload, 실패 graceful. 프롬프트 규칙7: `docs/routines/cs-tsmom-daily-report.md`. |
| `kis_lake_monitor.py` | KIS 1분봉 lake 누적 모니터 (#152). `lake/ohlcv/freq=*/...` walk → 종목별 (bar 수, 거래일, first/last ts) + 누적 진척도 (X/90) markdown digest 출력. `--out PATH` / `--telegram` 옵션. **KRX 6자리 종목만 필터링** (`scan_lake(krx_only=True)` default) — 동일 lake 디렉토리에 Binance backfill (BTCUSDT/ETHUSDT 등) 가 함께 저장돼 있어, 필터 없으면 weekly summary 가 5년 누적 (2192 거래일) 을 90일 target 에 잘못 매핑하는 버그 fix. `--include-non-krx` 로 opt-out. |
| `airborne_alert_daemon.py` | Airborne v1.1 USDT-perp 실시간 알림 데몬. top-N USDT-perp 유니버스 (`fetch_futures_24h_snapshot` + `top_n_by_volume`) → REST kline bootstrap (1h limit=100, 5m limit=50) → `BinanceMarketDataStream` (1h+5m kline + `!markPrice@arr@1s`) → 1h `k.x=True` 시 `evaluate_long_fire_v11` / `evaluate_short_fire_v11` → `dispatch_fire` (cooldown 4h, long/short 독립) → `observability.alerts.notify`. CLI: `--top-n 50` / `--dry-run` (stdout) / `--testnet`. **자동 매매 아님** — [[airborne-family-overview]] 가족 전체 rejected. 알림 본문에 "visual guide only" 명시. Spec: [[live-airborne-alert-daemon]]. **#380 (2026-06-07)**: `_norm_symbol()`/`_is_whitelisted()` — fire 가 Binance `1000SHIBUSDT` 로 와도 whitelist `SHIBUSDT`(Bitget)와 `1000` 프리픽스 정규화 후 매칭 (이전 직접비교라 "화이트리스트 외 종목" 오알림). `_KST_HOURS_SHORT_WL` 19h→24h 동기 (production.yaml override 반영). **venue 전환 (2026-06-07)**: `--venue {binance,bitget}` (기본 binance, env `AIRBORNE_VENUE`). `bitget` 이면 유니버스=`portfolio.bitget_top_dynamic.get_top_n_symbols(top_n)`, history=`brokers.bitget.market_ws.bootstrap_history` (interval 별 1h+5m) → `_bitget_bars_to_history` 어댑터로 `[KlineEvent]`(Decimal) → UTC float OHLCV DataFrame 변환 (Binance bootstrap 포맷과 동일). fire 평가/게이트/notice 는 venue 무관 — 데이터 소스만 swap. 실거래(broker=bitget-demo)와 동일 유니버스/가격 알림. **polling 모드만 지원** — `--venue bitget --mode ws` 는 ValueError 거부 (Bitget WS 미배선). pinned `always_include` 가 Bitget 에 없으면 bootstrap 빈 history → warmup graceful skip. Bitget 심볼은 멀티플라이어 프리픽스 없음 (Binance `1000SHIBUSDT` ↔ Bitget `SHIBUSDT`) — Bitget 포맷 그대로 fetch 에 사용해 데몬 내부 일관. |
| `ma_cross_alert_daemon.py` | Bitget USDT-perp 이동평균 골든/데드 크로스 알림 데몬 (1시간봉). 비트겟 거래량 top-N (`portfolio.bitget_top_dynamic.get_top_n_symbols`) → 1h 캔들 REST bootstrap (`brokers.bitget.market_ws.bootstrap_history`, limit=slow+50) → `_bitget_bars_to_history` 어댑터 (UTC float OHLCV) → 매 1h 경계 +30s polling (`_next_polling_wakeup`, 한국-IP-safe REST) → 새 봉마다 `detect_cross` (SMA fast/slow, 직전·현재 봉 상하 비교: golden=fast 가 slow 상향 돌파 / death=하향 돌파) → `evaluate_and_dispatch` (종목+방향 cooldown 6h) → `observability.alerts.notify` (LIVE 텔레그램 채널, airborne 알림과 동일). CLI: `--top-n 100` / `--fast 25` / `--slow 200` / `--dry-run` (stdout) / `--universe-refresh-hours 6`. 유니버스 재계산 시 removed drop·added 만 bootstrap → unchanged 종목 cooldown/last_eval 보존. **자동 매매 아님** — 시각적 가이드 알림 (불변식 #6, 주문/리스크 결정 없음). airborne_alert_daemon 의 `--venue bitget --mode polling` 인프라 차용, 신호만 MA 크로스로 교체. **24/7 운영은 도커 서비스 `ma-cross-daemon`** (`docker-compose.live.yml`, airborne-daemon 미러: restart unless-stopped, json-file 로그 10m×3, 256M) — `docker compose -f docker-compose.live.yml up -d ma-cross-daemon`. 테스트: `tests/scripts/test_ma_cross_alert_daemon.py`. |
| `ppp_signal_daemon.py` (2026-06-19) | **PPP 반전 스캘핑 페이퍼 시그널 데몬** (live-ppp-scalping-v1). ma_cross_alert_daemon 미러. 고변동 알트(기본 WLD/ORDI/WIF/1000PEPE) 5m, 매 5m 경계 +5s polling → Binance fapi REST klines fetch → `evaluate`: 과매도(<25) QPP(StochRSI) 골든크로스 **또는** 과매수(>75) 데드크로스 + 횡보레짐(Choppiness≥61.8) 반전 → `PPP-SIGNAL` 로그 + `observability.alerts.notify` + `dashboard.ppp_signal_store.PppSignalStore`(`logs/ppp/history.jsonl`) 누적. CLI `--symbols`/`--dry-run`/`--store`. ⚠️ **라이브 자동매매 미활성**(5y bench OOS 과적합 — 검증서 횡보레짐 필터가 PF 0.87→0.96~1.0 올렸으나 WLD 외·OOS test 에서 무너짐) → **페이퍼 OOS 데이터 수집 전용, 실발주 안 함.** 24/7 도커 서비스 `ppp-signal-daemon`. 대시보드 `/ppp` 페이지. |

## Shadow Live Loop (#80)

- `shadow_run.py` — Phase 1 Shadow Live Loop CLI 진입점
  - 사용법: `python scripts/shadow_run.py --symbols BTCUSDT,ETHUSDT --duration 8h`
  - BinancePublicFeed (aggTrade WS, 키 불요) 자동 연결
  - production.yaml 미존재 시 fallback (Phase 1 stub, #94 머지 후 활성화)
  - WAL/Lock: `logs/shadow/{run_id}/`
- `shadow_report.py` — WAL 파싱 + 일별 PnL → daily return 시계열 → `register_strategy_returns()` 호출
  - Sharpe 비교 4조건 강제 (동일 데이터 소스/슬리피지/수수료/사이징)
  - 4조건 불일치 시 fail 처리

### 운영 흐름

1. shadow_run.py 로 매일 장중 시간 실행 (10거래일 누적)
2. shadow_report.py 로 daily return + Sharpe 비교 → `02_implementation.md` 표 생성
3. Exit Criteria 5종 자동 검증

## Phase 2 운영 (#133)

`docker-compose.live.yml` 의 4 service 가 사용:
- `live-daemon` → `scripts/live_run.py --broker kis-paper-shadow ...` 직접 실행
- `report-cron` → `scripts/cron_loop.sh` (entrypoint) 가 sleep-loop 으로 KST 16:00 마다 `live_report.py` + `telegram_alert.py --report` 호출
- `telegram-notifier` → `scripts/telegram_alert.py --watch /data/logs` (WAL tail critical event 알림)
- `kis-1m-fetch-cron` → `scripts/kis_1m_fetch_loop.sh` (entrypoint) 가 평일 KST 16:00 `cron_fetch_kis_daily.py` 실행 + 매 결과 Telegram, 일요일 `kis_lake_monitor.py` 주간 누적 summary Telegram 발송 (#152)

운영 셋업: `scripts/local_setup_windows.md` 가이드 참조 (Windows 절전 끄기 + Docker Desktop 4GB+ 할당).
시크릿: `docs/work/active/000133-phase2-operation/SECRETS.md` — `.env` 변수 등록 절차.

### #238 follow-up — KIS-inert 루트원인 + 영구 이력 (live_run.py)

- `_wire_balance_provider(cfg, existing=None)` — `existing` 주어지면 그 provider 재사용 (대시보드의 warm `state.account_info_provider`). 미지정 시 fresh `AccountInfoProvider()` (레거시 byte-identical). **attached/smoke-dual 만** 공유 warm 인스턴스(`state.account_info_provider`) 재사용 — `_run_pipeline`(CLI 데몬)은 `existing=` 미전달이라 공유 안 함(의도적). "0 trades" 루트원인 차단의 *구조적·전 경로* 보강은 `snapshot_builder.py` last-known-good (관측된 실 equity 를 간헐 fetch 실패에도 유지); provider 공유는 attached 경로 보조 완화일 뿐.
- `config.on_snapshot_builder_ready` 콜백으로 live `SnapshotBuilder` 를 `DashboardState.snapshot_builder` 에 wire → `/api/venue_equity_status` 가 venue INERT 노출.
- `dashboard_state.log_dir` 를 `_run_pipeline`/smoke-dual 에서 `wal_path.parent.parent` 로 항상 set, `_serve()`(standalone) 는 `Path("logs/live")` seed → 거래 이력/전략별 포지션 영구·누적 (이전엔 미설정으로 매 run 소멸).

### Binance live-fill gap — `config.position_store` 배선 (live_run.py)

- **루트원인**: `binance/async_ws.stream_fills()` 가 production caller 0 → 라이브 `binance-testnet-shadow` 가 `order_filled` 를 한 번도 emit 안 함 → 대시보드가 제출 INTENT 만 영구 표시 (체결 안 보임), PnL/포지션/거래 0. 상세 → `src/live/.ai.md` "Binance live-fill gap".
- **배선**: 3 binance-capable 파이프라인 (`_run_pipeline_attached` / `_run_pipeline` / `_run_smoke_dual` 의 bnb branch) 이 이미 생성한 `StrategyPositionStore` 를 `config.position_store = position_store` 로 전달. `run_shadow_loop` 가 broker_mode=="binance-testnet-shadow" + `binance_adapter` 동시 충족 시 `run_binance_fill_consumer` background task spawn. smoke-dual KIS branch / paper / kis 경로는 `position_store` 미설정(또는 `binance_adapter` 부재) → fill-stream task 미생성 → byte-identical.

## 사용법
```bash
python scripts/fetch_candles.py --symbol BTCUSDT --interval 15m
python scripts/run_backtest.py --strategy momo-btc-v2 --data-dir lake/
python scripts/compare_momo_btc_v2_sizing.py --data-dir lake/
python scripts/train_metalabeler_btc.py --output-dir models/momo-btc-v2
python scripts/retrain_metalabeler.py --strategy-id momo-btc-v2 --lake-dir lake/ --bench
python scripts/shadow_run.py --symbols BTCUSDT,ETHUSDT --duration 8h
python scripts/shadow_run.py --symbols BTCUSDT --max-iterations 100  # 테스트용
python scripts/check_invariants.py --strict
python scripts/check_forbidden_files.py
python scripts/migrate_frontmatter.py --dry-run
python scripts/migrate_frontmatter.py --apply
python scripts/ontology_sync.py --check
python scripts/ontology_sync.py --push-graphdb   # GraphDB 로 A-Box 업로드 (#55)
python scripts/shacl_validate.py --strict
python scripts/graphdb_bootstrap.py              # 로컬 GraphDB 초기화 (#55)
# Phase 2 운영 (#133, Docker 컨테이너 내부 자동 실행 — 로컬 직접 실행은 디버깅용)
python scripts/telegram_alert.py --test          # Telegram bot 토큰 검증
python scripts/telegram_alert.py --watch /data/logs   # WAL tail 알림
python scripts/telegram_alert.py --report data/reports/2026-04-28.md   # 일일 리포트 요약 발송
docker compose -f docker-compose.live.yml up -d  # 운영 daemon 시작 (Stage 5 게이트 통과 후)
```

## 규칙
- 새 불변식 추가 시 `check_invariants.py`에 검사 함수를 작성하고 `CLAUDE.md`에도 명시
